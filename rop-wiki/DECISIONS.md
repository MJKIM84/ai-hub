# 결정 기록 (DECISIONS)

운영 전환 작업(2026-09-25~)에서 사람 확인 없이 내린 결정과 이유를 한 줄씩 남긴다. 사람이 다시 검토할 만한 것은 [BUILD_ASSUMPTIONS.md](BUILD_ASSUMPTIONS.md) 에도 [가정]으로 추가한다.

| # | 날짜 | 분류 | 결정 | 이유 |
|---|---|---|---|---|
| D-001 | 2026-09-25 | 1-1 원문 열람 진단 | 외부 열람 차단은 셸 스크립트만이 아니라 에이전트 웹 조회 도구(WebFetch)까지 같은 이그레스 프록시에서 막힌다고 판정했다 | curl 과 헤드리스 `claude -p` WebFetch 가 ref.gs1.org·arxiv.org·nist.gov·design.ros2.org·doi.org·crossref·openalex·wikipedia 등에서 같은 CONNECT 403(프록시 거부)을 냈다 |
| D-002 | 2026-09-25 | 1-1 원문 열람 진단 | raw.githubusercontent.com 은 셸과 에이전트 WebFetch 모두 열리고, github.com 페이지와 API 는 세션 저장소 범위 밖이라 막힌다. 웹 검색(WebSearch)은 서버 측이라 동작한다 | VDA5050 원문(raw) 200·208KB, gs1/EPCIS README 200, 헤드리스 WebFetch 가 VDA 5050 첫 제목을 읽음. github.com/VDA5050 은 403 |
| D-003 | 2026-09-25 | 1-1 원문 열람 | 원문 열람은 스크립트가 아니라 에이전트 WebFetch 로 하고, 공식 문서가 GitHub 에 있는 출처는 raw.githubusercontent.com 경로로 연다. 그 밖의 출처는 사람이 inbox/sources/ 에 넣은 파일로 연다 | 에이전트 도구가 열 수 있는 유일한 경로이며, 공식 저장소 원문이므로 원문 열람으로 인정할 수 있다 |
| D-004 | 2026-09-25 | 비용 | 비용 증가의 주원인은 호출마다 새로 쓰는 프롬프트 캐시(10만~27만 토큰)와 스토리텔러의 전체 페이지 재출력(최대 8.6만 출력 토큰)으로 판단했다 | 드라이런 응답의 usage: cache_read 는 Claude Code 기본 시스템 프롬프트(약 5~9천 토큰)만 적중했다 |
| D-005 | 2026-09-25 | 1-5 비용 | 공통 규칙과 역할 규칙(고정 텍스트)을 사용자 메시지가 아니라 `--append-system-prompt-file` 로 넘긴다 | 실험에서 같은 시스템 프롬프트의 두 번째 호출이 캐시 쓰기 56,684 → 0, 비용 $0.455 → $0.012 였다. 규칙은 한 글자도 바꾸지 않는다 |
| D-006 | 2026-09-25 | 1-1 원문 열람 | 열람 모드를 full / mirror_only / none 셋으로 나누고, mirror_only(일반 페이지 차단·GitHub 원문만 열림)는 override 없이 진행한다 | 페이지 열람 도구 자체는 동작하므로 "도구 없음"이 아니다. 출처별 fetched 표시와 코드 상한으로 품질을 지킨다 [가정] |
| D-007 | 2026-09-25 | 1-1 신뢰도 상한 | 원문 미열람 출처와 그 출처만 쓴 주장은 medium 이하, high 주장은 서로 다른 출처 2개 이상 중 1개 이상 원문 열람을 요구하고, 코드(validate_run.py research 단계)가 강제한다 | 사양서 5.2 high 정의(2개 이상 독립 출처)에 원문 확인을 더해 "검색 요약만으로 high"를 막는다 |
| D-008 | 2026-09-25 | 1-2 형식 검증 | 스토리텔러 직후 코드 형식 검증(절 제목·순서, 각주 정의, 태그 뒤 괄호, 본문 상태 줄, 퍼블리셔 2~4단계 사전 검사)을 두고, 실패하면 오류 목록을 붙여 스토리텔러에 형식 수정만 요청한다(최대 max_retries) | 퍼블리셔가 사후에 잡던 실패를 2차 검증 전에 잡아, 퍼블리셔는 검증을 통과한 산출물만 받는다 |
| D-009 | 2026-09-25 | 1-2 사후 보정 | 퍼블리셔의 태그 뒤 괄호 자동 정규화를 없애고 검사로만 남겼다 | 사후 보정 로직을 줄이라는 요청. 형식 검증이 앞 단계에서 같은 규칙을 오류로 잡는다 |
| D-010 | 2026-09-25 | 1-3 분량 | 세부영역 3~11절이 기준(4,000자)을 넘으면 5·9절을 뺀 큰 절부터 주제 페이지로 코드가 옮기고, 원 절에는 스토리텔러가 outline 에 적은 요약과 링크를 남긴다. 400자 이하 절은 옮기지 않는다 | 줄이지 않고 나눈다는 요청. 5절은 흐름 매트릭스 앵커의 기준, 9절은 ROP 직접 범위 판단이라 원 페이지에 둔다 [가정] |
| D-011 | 2026-09-25 | 1-3 분리 페이지 | 자동 분리로 생긴 주제 페이지는 주제 템플릿 10개 절을 코드가 채우고(본문은 옮긴 절 그대로), 하루 신규 주제 상한(new_topic_pages)에 세지 않는다 | 새 주장이 없는 분리이므로 예산의 취지(새 조사량 제한)와 다르다 [가정] |
| D-012 | 2026-09-25 | 1-5 차등 갱신 | update·category_link 실행에서 스토리텔러는 바뀐 절만 patches 로 보내고 코드가 현재 페이지에 적용한다 | 스토리텔러 출력 토큰(최대 8.6만)이 비용의 큰 몫이었다 |
| D-013 | 2026-09-25 | 1-6 정정 거절 | 검증 에이전트가 corrections_rejected(id·사유)를 내면 퍼블리셔가 inbox/corrections.md 상태를 rejected 로, 처리 메모에 사유를 쓰고 변경 이력에 남긴다 | 사양서 7.4 "처리 결과가 변경 이력에 남는다" |
| D-014 | 2026-09-25 | 3부 배치 | 배치 실행기(run_batch.py)가 에이전트 단계를 동시에 4개까지 돌리고 퍼블리셔는 하나씩 부른다. publish.py 는 전역 파일 잠금을 건다 | 대상이 50개 안팎이라 직렬로는 20시간이 넘는다. 퍼블리셔는 docs·git 을 바꾸므로 동시에 돌면 안 된다 |
| D-015 | 2026-09-25 | 3부 배치 | 병렬 실행이 같은 "다음 참고문헌 id"를 받으면 퍼블리셔가 반영 직전에 새 번호로 옮기고, 같은 URL 은 기존 id 를 재사용한다 | 공통 규칙 10(기존 각주 재사용)과 id 충돌 방지 |
| D-016 | 2026-09-25 | 2-4 트랙 비중 | track_runs_per_week 를 2 → 3 으로 올리고, 트랙 사이 배분은 track_weights(기본 모두 1)로 가중 순환한다. 트랙 요일은 화·목·토 | 트랙이 1개에서 3개가 되어, 트랙마다 주 1회를 기본으로 했다. 일반 순환(4회)과 정기 실행 몫을 남긴다 [가정] |
| D-017 | 2026-09-25 | 3부 대분류 | 대분류 연결 절은 새 실행 유형 category_link 로 채운다(7단계 그대로, 스토리텔러는 그 절만 patches). 소속 세부영역 4개가 모두 채워진 대분류는 순환 규칙에서도 자동 선정된다 | 기존 실행 유형 가운데 대분류 페이지의 한 절만 다루는 것이 없었다 |
| D-018 | 2026-09-25 | 3부 세부영역 | 시드 세부영역 27개는 "주제 조사"가 아니라 영역 심화(area_deep_dive)로 채운다 | 비어 있는 3~11절을 채우는 실행 유형이 영역 심화다(사양서 7.1 1주기). 요청의 목적(본문 게시)과 같다 |
| D-101 | 2026-09-25 | 운영: 설정 위치 | 알림·배포·cron 설정을 새 파일 config/ops.yaml 에 두고, settings.yaml 의 notify("none", 사양서 0장 원문 값)는 그대로 둔 채 실제 채널은 ops.yaml notify.channel 을 따른다(--status 가 두 값이 다르면 알려 준다) | settings.yaml 은 다른 작업자가 고치는 파일이고, 0장 원문 값을 바꾸지 않으면서 한 곳에서 스위치를 관리하기 위해서다 |
| D-102 | 2026-09-25 | 운영: 설정 오류 | ops.yaml 이 없거나 YAML 오류면 DEFAULTS(외부 스위치 모두 false)로 처리하고 _error 를 남긴다 | 설정 실수가 발송·배포·cron 을 켜는 쪽으로 흐르지 않게 한다 |
| D-103 | 2026-09-25 | 운영: 설정 읽기 경로 | ops.yaml 읽기(--get), 스위치 전환(--enable/--disable), 현황(--status) CLI 를 pipeline/lib/notify.py 에 둔다 | 셸 스크립트·cron 래퍼·GitHub 워크플로가 같은 읽기 코드를 쓰게 하고, 새 파일을 소유 범위 밖에 만들지 않기 위해서다 |
| D-104 | 2026-09-25 | 운영: 스위치 전환 | --enable/--disable 은 해당 절의 enabled(·channel·method) 줄만 정규식으로 바꾸고 주석 열 위치까지 보존한다 | yaml.dump 로 다시 쓰면 설명 주석이 모두 사라진다. 한 줄 명령으로 켜고 끌 수 있어야 한다 |
| D-105 | 2026-09-25 | 운영: 비용 합계 | cost_usd → total_cost_usd → cost(수 또는 total_usd·total_cost_usd·usd·total) → costs 순으로 찾고 없으면 줄을 생략한다 | 지금 summary.json 에는 비용 필드가 없고(agent_runner 는 호출별 total_cost_usd 만 로그), 다른 작업자가 어떤 키로 넣을지 모른다 |
| D-106 | 2026-09-25 | 운영: 메시지 구성 | 요청 필드(실행 id, 유형, 대상, 종료 상태, 생성/갱신, 비용, 로그 경로)에 보류 사유와 site_url 이 있을 때의 사이트 링크만 더하고 소요 시간·커밋은 넣지 않는다 | 짧은 알림이 목적이고, 보류 사유는 사람이 바로 판단해야 하는 정보다 |
| D-107 | 2026-09-25 | 운영: 실행 유형 이름 | 유형 한국어 이름은 호출 시 lib/runs.py 의 RUN_TYPE_KO 를 읽고 실패할 때만 사본을 쓴다 | 다른 작업자가 실행 유형(category_link)을 추가하고 있어 사본은 어긋난다(테스트로 확인) |
| D-108 | 2026-09-25 | 운영: 비밀값 | 웹훅 URL·SMTP 계정·비밀번호는 환경변수(ROP_SLACK_WEBHOOK_URL, ROP_SMTP_USERNAME, ROP_SMTP_PASSWORD)에서만 읽고, 오류 문구에 들어간 URL·계정·비밀번호는 ***로 가린다 | 비밀값을 파일·로그·일일 로그(커밋됨)에 남기지 않기 위해서다 |
| D-109 | 2026-09-25 | 운영: 슬랙 보안 | 웹훅 URL 은 https:// 만 허용한다 | 평문 전송으로 웹훅 토큰이 새는 것을 막는다 |
| D-110 | 2026-09-25 | 운영: 켜졌는데 비밀값 없음 | enabled: true 인데 환경변수가 비었으면 skipped 가 아니라 failed 로 돌려준다 | 설정 누락이 조용히 묻히지 않고 로그에 실패로 보이게 한다 |
| D-111 | 2026-09-25 | 운영: 이메일 연결 | use_tls 는 STARTTLS(587), 465 용으로 use_ssl 을 더했다. 계정·비밀번호는 둘 다 있거나 둘 다 없어야 한다 | 두 방식이 흔하고, 한쪽만 설정된 상태는 거의 항상 실수다 |
| D-112 | 2026-09-25 | 운영: 채널 both | 한 채널이라도 실패하면 status failed, detail 에 채널별 결과를 모두 적는다 | 상태값을 요청한 네 가지로 유지하면서 부분 실패를 숨기지 않는다 |
| D-113 | 2026-09-25 | 운영: 예외 처리 | send() 는 모든 예외를 잡아 failed 로 돌려준다(망가진 summary 포함) | 알림 실패가 게시·커밋을 되돌리면 안 된다(publish.py 는 커밋 뒤 오류를 경고로만 처리) |
| D-114 | 2026-09-25 | 운영: 시험 발송 | --test 는 실행 id 앞에 [시험]을 붙이고, --mock 은 enabled 를 무시하고 자리표시 URL·주소로 모의 전송 내용만 출력한다. 실제 시험 발송은 --test(enabled 필요) 또는 --test --force | 실제 채널로 가도 시험 메시지임을 알 수 있고, 모의 실행은 어떤 설정에서도 네트워크를 쓰지 않는다 |
| D-115 | 2026-09-25 | 운영: 배포 방식 | deploy.method 기본값은 gh-deploy(서버가 gh-pages 푸시), GitHub Actions 템플릿은 대안(method: actions)으로 두고 둘 중 하나만 쓰게 한다 | Pages 배포 원천은 하나뿐이고, 요청한 deploy_site.sh --yes 흐름과 cron 서버 운영이 한 흐름으로 이어진다 |
| D-116 | 2026-09-25 | 운영: 워크플로 스위치 | 템플릿 워크플로는 ops.yaml 의 deploy.enabled=true 이고 method=actions 일 때만 configure/upload/deploy 단계를 실행하고, 아니면 빌드만 한다 | 켜고 끄는 스위치를 ops.yaml 한 곳에 모은다 |
| D-117 | 2026-09-25 | 운영: 워크플로 트리거 | 기본은 workflow_dispatch 만 두고 push 트리거(branches: [main], paths: rop-wiki/**)는 주석으로 둔다 | 복사만으로 자동 배포가 시작되지 않게 한다. 서버가 어떤 브랜치에 푸시할지 아직 정해지지 않았다 |
| D-118 | 2026-09-25 | 운영: 워크플로 위치 | 템플릿은 rop-wiki/deploy/github-pages.yml 에만 두고 .github/ 에는 아무것도 만들지 않았다 | .github/workflows/ 에 있으면 푸시 즉시 실제 원격 배포가 될 수 있다 |
| D-119 | 2026-09-25 | 운영: 액션 버전 | checkout@v4, setup-python@v5, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4 를 쓴다(README 에 복사 시 최신 주 버전으로 올려도 된다고 적음) | 공식 문서에 널리 쓰인 조합이며 서로 호환이 확인된 주 버전이다 |
| D-120 | 2026-09-25 | 운영: 배포 전 확인 | deploy_site.sh 는 로컬 빌드(settings.site_build_cmd)가 실패하면 배포하지 않고(exit 1), git 원격이 없으면 exit 4, 요청한 명령(gh-deploy --force --remote-branch gh-pages)은 --no-history 없이 그대로 쓴다 | 깨진 사이트를 올리지 않고, 요청 명령과 다르게 gh-pages 이력을 지우지 않는다 |
| D-121 | 2026-09-25 | 운영: 자동 배포 시점 | gh-deploy 방식의 매일 배포는 cron 래퍼가 정규 실행 성공(exit 0) 뒤 부른다(deploy.after_daily_run). 수동 run_daily.sh 실행은 배포하지 않는다 | publish.py 를 고치지 않고 운영(cron) 경로에서만 외부에 영향을 준다 |
| D-122 | 2026-09-25 | 운영: 겹침 방지 | crontab 줄에 flock -n -E 75 <위키>/runs/.lock 을 두고, 잠금 충돌(75)일 때만 두 번째 명령(cron_wrapper.sh --lock-busy)이 "건너뜀"을 cron.log 에 남긴다 | flock 은 프로세스가 죽으면 커널이 잠금을 풀어 오래된 잠금 파일 문제가 없고, 건너뛴 사실이 로그에 남아야 한다 |
| D-123 | 2026-09-25 | 운영: 래퍼 종료 코드 | cron_wrapper.sh 는 run_daily.sh 의 종료 코드를 그대로 돌려주고 뜻(0 완료, 2 보류·설정 오류, 3 준비 중단, 4 퍼블리셔 실패, 124 시간 초과)을 로그에 적는다 | -E 75 로 잠금 충돌을 구분하므로 실행 실패와 섞이지 않는다 |
| D-124 | 2026-09-25 | 운영: 로그 순환 | cron.log 가 5MB 를 넘으면 실행 시작 전에 cron.log.1 로 옮기는 1세대 순환을 쓴다(끝 5MB 잘라 남기기 대신) | 줄 중간이 잘리지 않고, 매 실행마다 큰 파일을 다시 쓰지 않는다. 최대 약 10MB 로 제한된다 |
| D-125 | 2026-09-25 | 운영: 실행 시간 상한 | 래퍼가 run_daily.sh 를 timeout --kill-after=300 14400(4시간, cron.max_runtime_sec)으로 실행한다 | 걸린 실행이 잠금을 계속 잡으면 다음 날부터 모든 실행이 건너뛰어진다. 정상 실행은 약 30분이고 에이전트 호출 상한이 30분이다 |
| D-126 | 2026-09-25 | 운영: CRON_TZ | 이 서버(Ubuntu 24.04, cron 3.0pl1)의 cron 바이너리에 CRON_TZ 문자열이 없어 무시된다고 판단하고, tz_mode auto 가 지원 여부를 보고 지원하지 않으면 서버 시간대로 환산해 등록한다(UTC 서버: 0 21 * * *) | 기존 등록 블록은 UTC 서버에서 06:00 UTC(=15:00 KST)에 돌았을 것이다. 일광 절약 시간이 있는 시간대면 경고한다 |
| D-127 | 2026-09-25 | 운영: CRON_TZ 줄 | server 모드에서는 CRON_TZ= 줄을 두지 않고 주석으로만 설명한다 | 환산 시각과 CRON_TZ 줄이 함께 있으면 읽는 사람이 오해하고, cronie 로 옮기면 이중 적용된다 |
| D-128 | 2026-09-25 | 운영: 블록 위치 | 등록 블록은 언제나 crontab 맨 끝에 두고 블록 뒤에 다른 항목을 두지 말라고 주석에 적는다 | CRON_TZ·PATH 줄은 그 뒤의 모든 줄에 적용된다 |
| D-129 | 2026-09-25 | 운영: PATH 줄 | command -v 로 claude·mkdocs·python3·git 의 디렉터리를 PATH 에 넣는다(git 추가) | 퍼블리셔 7단계 커밋이 git 을 쓴다 |
| D-130 | 2026-09-25 | 운영: flock 없음 | flock(util-linux)이 없으면 실제 등록을 거부한다(미리 보기는 가능) | 겹침 방지 없이 운영 cron 을 등록하지 않는다 |
| D-131 | 2026-09-25 | 운영: 등록 스위치 | cron.enabled 가 false 면 install_cron.sh 가 exit 3 으로 거부하고(--force 로 한 번 무시), --dry-run 은 언제나 되며 uninstall_cron.sh 는 스위치와 관계없이 해제한다 | 켜는 동작만 막고 미리 보기·끄는 동작은 막지 않는다 |
| D-132 | 2026-09-25 | 운영: 환경 파일 | ~/.config/rop-wiki/env 가 있으면 set -a 로 읽고, 권한이 600/400 이 아니면 경고만 하며, 로그에는 알려진 변수 이름의 설정 여부만 남긴다 | 비밀값 파일을 권장 권한으로 유도하되 실행을 막지 않고, 값이 cron.log 에 남지 않게 한다 |
| D-133 | 2026-09-25 | 운영: MAILTO | crontab 의 MAILTO 는 건드리지 않는다 | 다른 cron 항목에도 적용되는 전역 설정이다. 출력은 래퍼가 cron.log 로 모두 보낸다 |
| D-134 | 2026-09-25 | 운영: 조기 실패 알림 | 실행 id 가 생기기 전의 실패(설정 파일 오류 등)는 알림 대상에서 뺐다(cron.log 에만 남음) | 보류·준비 중단·퍼블리셔 실패는 publish.py --log-only 경로에서 알릴 수 있고, 래퍼에서 따로 알리면 중복 알림이 생긴다 |
| D-135 | 2026-09-25 | 운영: 테스트 범위 | ops.yaml 이 꺼져 있는지 강제하는 테스트는 두지 않고, 코드 기본값(DEFAULTS)이 꺼짐인지와 ops.yaml 이 읽히는지만 검사한다 | 사용자가 스위치를 켜면 검사 전체가 깨지면 안 된다 |
| D-136 | 2026-09-25 | 운영: 테스트 격리 | 셸 테스트는 가짜 crontab·mkdocs·git 과 ROP_OPS_CONFIG·ROP_CRON_LOG·ROP_ENV_FILE·ROP_RUN_DAILY 환경변수로 격리한다 | 실제 crontab·원격 저장소·네트워크·Claude 호출을 건드리지 않고 등록·해제·잠금·배포 경로를 끝까지 실행해 볼 수 있다 |
| D-137 | 2026-09-25 | 운영: 실제 crontab | 실제 crontab 으로 거부(exit 3)·--force 등록·교체·해제를 확인한 뒤, 작업 전에 있던 옛 rop-wiki 블록까지 해제해 crontab 을 비워 두었다 | 요청대로 rop-wiki 블록 없이 끝내야 하고, 옛 블록은 CRON_TZ 미지원 cron 에서 잘못된 시각으로 등록된 것이었다 |
| D-138 | 2026-09-25 | 운영: 추적 제외 파일 | runs/cron.log·cron.log.1 을 .gitignore 에 넣을 것을 메인 작업자에게 요청한다(runs/.lock 은 다른 작업자가 이미 추가) | .gitignore 는 이 작업의 소유 파일이 아니고, 퍼블리셔는 runs/<id>/ 만 커밋해 두 파일이 미추적으로 남는다 |
| D-139 | 2026-09-25 | 운영: 검사 스크립트 | test_ops.py 를 run_all.sh 에 넣는 일은 메인 작업자에게 맡긴다 | run_all.sh 는 이 작업의 소유 파일이 아니다 |
| D-201 | 2026-09-25 | 원문: 미러 탐색 | 저장소 경로는 `git clone --depth 1 --filter=blob:none --no-checkout` 으로 파일 목록만 받아 찾고, 등록 전 raw.githubusercontent.com 에서 실제로 받아 200·300바이트 이상인지 확인했다(생성 스크립트 scratchpad/wave1/gen_mirrors.py) | github.com 페이지·API 는 막혀 있지만 공개 저장소의 git 프로토콜은 열려 있어 추측 없이 정확한 경로를 얻을 수 있다 |
| D-202 | 2026-09-25 | 원문: 미러 목록 | config/source_mirrors.yaml 에 `relation`(original·official_artifact·related) 필드를 더했다 | 같은 문서의 원본과 같은 표준의 스키마·온톨로지, 제3자 구현을 구분하지 않으면 "원문 열람"이 과대 표시된다 |
| D-203 | 2026-09-25 | 원문: 미러 목록 | 확인하지 못한 정본 URL(IDTA 사이트의 템플릿 페이지 등)은 match 에 넣지 않고 GitHub 경로만 match 로 두었다 | 추측한 URL 을 목록에 두지 않는다는 규칙(확인한 것만 등록) |
| D-204 | 2026-09-25 | 원문: 미러 목록 | 300바이트 미만 파일 2개(admin-shell-io published/README.md, ARIAC_docs README.md)는 목록에서 뺐다 | 실제 내용이 거의 없어 원문 경로로 쓸 가치가 없다 |
| D-205 | 2026-09-25 | 원문: 미러 목록 | OpenEPCIS 문서(ref-013), EPC TDS(ref-021), Plattform I4.0(ref-035), 논문(arXiv·Nature·Frontiers·KCI)은 미러를 두지 않았다 | GitHub 공식 원문을 찾지 못했다(gs1/TDT 는 변환 규칙 저장소라 TDS 본문이 아니다) |
| D-206 | 2026-09-25 | 원문: 미러 판정 | EPCIS 온톨로지(ref-003), CBV(ref-014), VDA 5050 2.0.0 태그(ref-022), W3C SSN 편집자 초안(ref-030), MassRobotics 표준 저장소(ref-033), OPC UA 로보틱스 노드셋(ref-034)은 official_artifact 로 두고 원문 열람으로 치지 않았다 | 저장소 README 가 "초안"이라 하거나(EPCIS), 머리말에 'RELEASE CANDIDATE' 가 남아 있거나(VDA 2.0.0), 편집자 초안이라 게시판과 같다고 확인할 수 없다. 인용한 페이지 본문과 같은 글이 아니다 |
| D-207 | 2026-09-25 | 원문: 미러 판정 | IEEE 1872.2 OWL(hsu-aut), KnowRob·SOMA OWL, VDA 5050 3.0.0 태그(보도자료 ref-032)는 related 로 두었다 | 제3자 구현이거나 논문·보도자료가 설명하는 대상일 뿐 인용 문서 자체가 아니다 |
| D-208 | 2026-09-25 | 원문: 미러 판정 | original 항목이 URL 접두어로만 맞으면(같은 사이트의 다른 페이지) related 로 낮춘다 | 책 목차(SUMMARY.md)나 문서 첫 페이지(index.rst)를 다른 장의 원문으로 잘못 저장하는 것을 막는다 |
| D-209 | 2026-09-25 | 원문: 미러 판정 | github.com/<owner>/<repo>/blob/<ref>/<path> 는 목록에 없어도 raw 경로로 바꿔 original 로 본다 | 같은 파일의 다른 표현이므로 확인이 필요 없다(ref-031) |
| D-210 | 2026-09-25 | 원문: 원문 열람 규칙 | fetched 는 "본문을 실제로 읽음"만 뜻한다. URL 이 열리는 것, 검색 스니펫, 공식 산출물·관련 파일을 연 것은 열람이 아니다 | 사용자 규칙(원문 미열람 출처는 medium 상한)을 코드로 강제하려면 기준이 좁고 분명해야 한다 |
| D-211 | 2026-09-25 | 원문: 원문 열람 규칙 | 에이전트의 webfetch 표시는 web_fetch_available: false 실행에서 받아들이지 않고, github_raw 표시는 fetch_url 이 raw 경로이면서 official_artifact·related 미러가 아닐 때(또는 original 미러가 있을 때)만 받아들인다 | 이 환경은 WebFetch 점검이 실패해도 raw.githubusercontent.com 은 열린다(D-002). 반대로 점검 실패 실행에서 일반 사이트를 열었다는 표시는 모순이다 |
| D-212 | 2026-09-25 | 원문: 원문 열람 규칙 | data/source_texts 에 원문 텍스트가 있으면 에이전트 표시가 없어도 fetched true(경로는 텍스트의 fetched_via) | 텍스트가 프롬프트 입력으로 들어가므로 원문을 읽은 것과 같다 |
| D-213 | 2026-09-25 | 원문: 원문 열람 규칙 | apply_fetch_caps 는 출처의 source_unopened 를 fetched 의 반대로 맞추고, 발견 사항의 source_unopened 도 근거 출처가 모두 미열람일 때 true 로 맞춘다 | 퍼블리셔 _apply_references 가 source_unopened 로 "원문 열람" 칸과 각주의 "(원문 미열람)"을 정하므로 두 값이 어긋나면 안 된다 |
| D-214 | 2026-09-25 | 원문: 원문 열람 규칙 | 같은 출처 id 를 두 번 적은 발견 사항은 출처 1개로 센다 | high 는 서로 다른 출처 2개 이상이라는 규칙 |
| D-215 | 2026-09-25 | 원문: URL 확인 | 정책 차단 판정: CONNECT 403/407("Tunnel connection failed"), 평문 HTTP 403 + x-deny-reason 헤더, 본문 "Host not in allowlist"·"not enabled for this session". 그 밖의 403 은 오류 | 실제 응답을 확인했다: 평문 HTTP 는 투명 프록시가 403+x-deny-reason, github.com 은 403 JSON, 그 밖의 HTTPS 는 CONNECT 403. 사이트 자체의 403(봇 차단)과 섞지 않는다 |
| D-216 | 2026-09-25 | 원문: URL 확인 | HEAD 가 정책 차단이 아닌 403·405·501 이면 GET 으로 다시 확인하고, 정책 차단이면 다시 요청하지 않는다 | HEAD 를 거부하는 사이트가 있고, github.com 은 GET 본문으로만 정책 거부를 알 수 있다. 프록시 정책 거부는 재시도하지 않는다(프록시 안내) |
| D-217 | 2026-09-25 | 원문: URL 확인 | 정본이 열리지 않으면(정책 차단뿐 아니라 오류·없음도) 미러를 확인하고, 정본이 열리면 미러는 기록만 하고 요청하지 않는다 | 원문 경로가 살아 있는지 알려 주되 불필요한 요청은 줄인다 |
| D-218 | 2026-09-25 | 원문: URL 확인 | data/url_check.json 을 기본 출력으로 하고 `--refs` 로 일부만 확인하면 기존 결과에 합친다 | 사양이 data/url_check.json 을 요구했고, 부분 확인이 다른 참고문헌 결과를 지우면 안 된다 |
| D-219 | 2026-09-25 | 원문: URL 확인 | 표준 출력 마지막 줄 형식 `[check_urls] N건: …` 은 유지했다 | run_daily.sh weekly_checks 가 이 줄을 grep 한다 |
| D-220 | 2026-09-25 | 원문: URL 확인 | 확인 시각은 Asia/Seoul 로 기록한다 | 위키의 날짜 기준 시간대(settings.timezone) |
| D-221 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문 열람 상태 블록을 비고 절 끝에 `<!-- source-fetch:start/end -->` 마커로 둔다(auto: 접두어 아님) | autoregion.AUTO_KEYS 는 다른 담당이 고치는 중이고, 등록되지 않은 auto 키는 퍼블리셔 strict 갱신을 실패시킨다. 필요하면 메인이 auto 키로 등록할 수 있다 |
| D-222 | 2026-09-25 | 원문: 참고문헌 페이지 | 시드 10건을 5개 절(서지 정보·요약·인용된 페이지·각주 형식·비고)로 바꾸면서 "원문 열람:" 문단은 비고로 옮기고, 요약 끝에 "등록: 구축 시드(분류 원문 12장 참고 자료 N번, 2026-09-24) · 검증: 시드 상태(검증 실행 전)" 줄을 더했다 | 퍼블리셔 형식의 "등록 실행 · 검증" 줄에 대응하는 사실만 적었다. [분류원문] 줄·표·auto 영역·각주 줄은 글자 그대로 둔다 |
| D-223 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문을 열람하게 된 페이지의 "원문 미열람" 비고 문단은 지우지 않고 "등록 당시 기록: … → YYYY-MM-DD 에 <경로> 경로로 원문을 열람했다"로 바꾼다 | 기존 사실을 지우지 않으면서 현재 상태와 모순되지 않게 한다 |
| D-224 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문 미열람 출처의 각주 형식 줄에 " (원문 미열람)"을 붙였다(시드 10건은 없었다). 열람한 출처는 떼고 접근일을 수집일로 바꾼다 | 공통 규칙 6절·템플릿이 요구하는 표기이며 스토리텔러가 이 줄을 그대로 복사한다 |
| D-225 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문을 열람(fetched)한 출처만 신뢰도 medium 상한을 풀고 유형 기준값(표준·논문·정부·연구기관·오픈소스 문서 high)으로 올린다. URL 이 열리는 것만으로는 올리지 않는다(기존 --apply-url-check 동작 변경) | 사용자 규칙 "fetched false 출처는 medium 상한"과 같은 기준. URL 열림은 실재 확인일 뿐 내용 확인이 아니다 |
| D-226 | 2026-09-25 | 원문: 참고문헌 페이지 | 이번에 원문 열람(github_raw)으로 표시한 것은 relation original 이 확인된 8건(ref-004, 008, 009, 010, 023, 031, 039, 040)뿐이다. 나머지 35건은 fetched: false | curl 로 200 과 본문을 확인하고 ingest 로 data/source_texts 에 저장한 것만 열람으로 인정했다 |
| D-227 | 2026-09-25 | 원문: 참고문헌 페이지 | 프런트매터에 fetched·fetched_via·url_status·url_checked(·열람 시 source_text, reliability_by_type)를 더했고 바뀐 페이지는 updated=2026-09-25, version +1 | 기존 apply_url_check 의 갱신 관례(updated·version)를 따랐다 |
| D-228 | 2026-09-25 | 원문: 참고문헌 페이지 | 미열람 행에 이미 붙어 있던 설명("(구축 환경의 네트워크 정책으로 URL 을 열지 못함)")은 그대로 둔다 | 기존 사실 보존 |
| D-229 | 2026-09-25 | 원문: 파일 소유 | 이번 작업의 반영(ingest --mirrors, --apply-url-check)은 data/changelog.json 에 쓰지 않았다(--no-changelog). 기능 자체는 기본값으로 변경 이력을 남긴다 | data/changelog.json 은 다른 담당도 고치는 공용 파일이라 동시 수정 충돌을 피했다. 운영에서는 변경 이력이 있어야 추적할 수 있다 |
| D-230 | 2026-09-25 | 원문: 파일 소유 | scaffold.py main() 대신 apply_url_check() 를 직접 불러 반영했다 | main() 은 끝에 auto 영역 전체와 mkdocs.yml 을 다시 쓰는데, 이는 다른 담당이 고치는 중인 파일이다 |
| D-231 | 2026-09-25 | 원문: 수집(inbox) | manifest 항목에 file 필드를 필수로 두고, 처리 뒤 manifest 는 고치지 않는다(파일 이동 + sha256 비교로 멱등) | 사람이 쓴 YAML(주석 포함)을 스크립트가 다시 쓰지 않는다 |
| D-232 | 2026-09-25 | 원문: 수집(inbox) | 참고문헌 연결 우선순위: ref_id → URL 정규형 일치(scheme·www·끝 슬래시·#조각 무시) → 없으면 src-<slug> 로 저장만 | 사람이 명시한 값이 우선이고, 참고문헌이 나중에 생기면 다음 실행의 sync 가 연결한다 |
| D-233 | 2026-09-25 | 원문: 수집(inbox) | 추출 텍스트가 20자 미만이면 실패로 보고 파일을 inbox 에 남긴다 | 스캔 이미지 PDF 를 "원문 열람"으로 잘못 표시하지 않는다 |
| D-234 | 2026-09-25 | 원문: PDF | poppler-utils 를 apt-get 으로 설치했다(apt-get update 뒤 성공). pypdf 는 별도 프로세스에서 cryptography 를 막고 불러 쓰는 대체 경로로 둔다 | 시스템 cryptography 가 import 중 pyo3 패닉(BaseException)을 내 같은 프로세스에서는 잡을 수 없다. 별도 프로세스면 패닉이 격리된다 |
| D-235 | 2026-09-25 | 원문: 텍스트 저장 | 원문 텍스트는 가공 없이(마크다운·rst 그대로, HTML 만 텍스트로) 저장하고 메타데이터는 index.json 에만 둔다 | sha256 로 변경을 판정하고 원문을 그대로 보존한다 |
| D-236 | 2026-09-25 | 원문: 프롬프트 입력 | select_texts_for_prompt 는 인용 출처는 앞부분, 키워드 일치 텍스트는 키워드 주변(±400자) 발췌로, 남은 예산을 남은 항목 수로 나눠 준다(최소 1500자) | 인용 출처가 먼저라는 사양과 프롬프트 크기(비용, D-004) 제한을 함께 지킨다 |
| D-237 | 2026-09-25 | 원문: 테스트 | 테스트는 임시 폴더(TemporaryDirectory)와 가짜 urlopen 만 쓰고, 저장소 안의 실제 미러 파일은 읽기만 한다. PDF 테스트는 도구가 없으면 건너뛴다 | 저장소에 파일을 남기지 않고 네트워크에 의존하지 않는다 |
