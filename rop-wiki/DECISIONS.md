# 결정 기록 (DECISIONS)

운영 전환 작업(2026-09-25~)에서 사람 확인 없이 내린 결정과 이유를 한 줄씩 남긴다. 사람이 다시 검토할 만한 것은 [BUILD_ASSUMPTIONS.md](BUILD_ASSUMPTIONS.md) 에도 [가정]으로 추가한다.

| # | 날짜 | 분류 | 결정 | 이유 |
|---|---|---|---|---|
| D-001 | 2026-09-25 | 1-1 원문 열람 진단 | 외부 열람 차단은 셸 스크립트만이 아니라 에이전트 웹 조회 도구(WebFetch)까지 같은 이그레스 프록시에서 막힌다고 판정했다 | curl 과 헤드리스 `claude -p` WebFetch 가 ref.gs1.org·arxiv.org·nist.gov·design.ros2.org·doi.org·crossref·openalex·wikipedia 등에서 같은 CONNECT 403(프록시 거부)을 냈다 |
| D-002 | 2026-09-25 | 1-1 원문 열람 진단 | raw.githubusercontent.com 은 셸과 에이전트 WebFetch 모두 열리고, github.com 페이지와 API 는 세션 저장소 범위 밖이라 막힌다. 웹 검색(WebSearch)은 서버 측이라 동작한다 | VDA5050 원문(raw) 200·208KB, gs1/EPCIS README 200, 헤드리스 WebFetch 가 VDA 5050 첫 제목을 읽음. github.com/VDA5050 은 403 |
| D-003 | 2026-09-25 | 1-1 원문 열람 | 원문 열람은 스크립트가 아니라 에이전트 WebFetch 로 하고, 공식 문서가 GitHub 에 있는 출처는 raw.githubusercontent.com 경로로 연다. 그 밖의 출처는 사람이 inbox/sources/ 에 넣은 파일로 연다 | 에이전트 도구가 열 수 있는 유일한 경로이며, 공식 저장소 원문이므로 원문 열람으로 인정할 수 있다 |
| D-004 | 2026-09-25 | 비용 | 비용 증가의 주원인은 호출마다 새로 쓰는 프롬프트 캐시(10만~27만 토큰)와 스토리텔러의 전체 페이지 재출력(최대 8.6만 출력 토큰)으로 판단했다 | 드라이런 응답의 usage: cache_read 는 Claude Code 기본 시스템 프롬프트(약 5~9천 토큰)만 적중했다 |
| D-005 | 2026-09-25 | 1-5 비용 | 공통 규칙과 역할 규칙(고정 텍스트)을 사용자 메시지가 아니라 `--append-system-prompt-file` 로 넘긴다 | 실험에서 같은 시스템 프롬프트의 두 번째 호출이 캐시 쓰기 56,684 → 0, 비용 $0.455 → $0.012 였다. 규칙은 한 글자도 바꾸지 않는다 |
| D-006 | 2026-09-25 | 1-1 원문 열람 | 열람 모드를 full / mirror_only / none 셋으로 나누고, mirror_only(일반 페이지 차단·GitHub 원문만 열림)는 override 없이 진행한다 | 페이지 열람 도구 자체는 동작하므로 "도구 없음"이 아니다. 출처별 fetched 표시와 코드 상한으로 품질을 지킨다 [가정] |
| D-007 | 2026-09-25 | 1-1 신뢰도 상한 | 원문 미열람 출처와 그 출처만 쓴 주장은 medium 이하, high 주장은 서로 다른 출처 2개 이상 중 1개 이상 원문 열람을 요구하고, 코드(validate_run.py research 단계)가 강제한다 | 사양서 5.2 high 정의(2개 이상 독립 출처)에 원문 확인을 더해 "검색 요약만으로 high"를 막는다 |
| D-008 | 2026-09-25 | 1-2 형식 검증 | 스토리텔러 직후 코드 형식 검증(절 제목·순서, 각주 정의, 태그 뒤 괄호, 본문 상태 줄, 퍼블리셔 2~4단계 사전 검사)을 두고, 실패하면 오류 목록을 붙여 스토리텔러에 형식 수정만 요청한다(최대 max_retries) | 퍼블리셔가 사후에 잡던 실패를 2차 검증 전에 잡아, 퍼블리셔는 검증을 통과한 산출물만 받는다 |
| D-009 | 2026-09-25 | 1-2 사후 보정 | 퍼블리셔의 태그 뒤 괄호 자동 정규화를 없애고 검사로만 남겼다 | 사후 보정 로직을 줄이라는 요청. 형식 검증이 앞 단계에서 같은 규칙을 오류로 잡는다 |
| D-010 | 2026-09-25 | 1-3 분량 | 세부영역 3~11절이 기준(4,000자)을 넘으면 5·9절을 뺀 큰 절부터 주제 페이지로 코드가 옮기고, 원 절에는 스토리텔러가 outline 에 적은 요약과 링크를 남긴다. 400자 이하 절은 옮기지 않는다 | 줄이지 않고 나눈다는 요청. 5절은 흐름 매트릭스 앵커의 기준, 9절은 ROP 직접 범위 판단이라 원 페이지에 둔다 [가정] |
| D-011 | 2026-09-25 | 1-3 분리 페이지 | 자동 분리로 생긴 주제 페이지는 주제 템플릿 10개 절을 코드가 채우고(본문은 옮긴 절 그대로), 하루 신규 주제 상한(new_topic_pages)에 세지 않는다 | 새 주장이 없는 분리이므로 예산의 취지(새 조사량 제한)와 다르다 [가정] |
| D-012 | 2026-09-25 | 1-5 차등 갱신 | update·category_link 실행에서 스토리텔러는 바뀐 절만 patches 로 보내고 코드가 현재 페이지에 적용한다 | 스토리텔러 출력 토큰(최대 8.6만)이 비용의 큰 몫이었다 |
| D-013 | 2026-09-25 | 1-6 정정 거절 | 검증 에이전트가 corrections_rejected(id·사유)를 내면 퍼블리셔가 inbox/corrections.md 상태를 rejected 로, 처리 메모에 사유를 쓰고 변경 이력에 남긴다 | 사양서 7.4 "처리 결과가 변경 이력에 남는다" |
| D-014 | 2026-09-25 | 3부 배치 | 배치 실행기(run_batch.py)가 에이전트 단계를 동시에 4개까지 돌리고 퍼블리셔는 하나씩 부른다. publish.py 는 전역 파일 잠금을 건다 | 대상이 50개 안팎이라 직렬로는 20시간이 넘는다. 퍼블리셔는 docs·git 을 바꾸므로 동시에 돌면 안 된다 |
| D-015 | 2026-09-25 | 3부 배치 | 병렬 실행이 같은 "다음 참고문헌 id"를 받으면 퍼블리셔가 반영 직전에 새 번호로 옮기고, 같은 URL 은 기존 id 를 재사용한다 | 공통 규칙 10(기존 각주 재사용)과 id 충돌 방지 |
| D-016 | 2026-09-25 | 2-4 트랙 비중 | track_runs_per_week 를 2 → 3 으로 올리고, 트랙 사이 배분은 track_weights(기본 모두 1)로 가중 순환한다. 트랙 요일은 화·목·토 | 트랙이 1개에서 3개가 되어, 트랙마다 주 1회를 기본으로 했다. 일반 순환(4회)과 정기 실행 몫을 남긴다 [가정] |
| D-017 | 2026-09-25 | 3부 대분류 | 대분류 연결 절은 새 실행 유형 category_link 로 채운다(7단계 그대로, 스토리텔러는 그 절만 patches). 소속 세부영역 4개가 모두 채워진 대분류는 순환 규칙에서도 자동 선정된다 | 기존 실행 유형 가운데 대분류 페이지의 한 절만 다루는 것이 없었다 |
| D-018 | 2026-09-25 | 3부 세부영역 | 시드 세부영역 27개는 "주제 조사"가 아니라 영역 심화(area_deep_dive)로 채운다 | 비어 있는 3~11절을 채우는 실행 유형이 영역 심화다(사양서 7.1 1주기). 요청의 목적(본문 게시)과 같다 |
| D-019 | 2026-09-25 | 3부 배치 | 스토리텔러 호출 시각의 커밋을 runs/<id>/base.json 에 남기고, 퍼블리셔가 그 뒤 다른 실행이 바꾼 페이지를 git merge-file 로 3-way 병합한다. 충돌이면 대상 페이지는 실패(재시도), 대상이 아닌 페이지는 이 실행의 변경을 뺀다 | 병렬 배치에서 먼저 게시한 실행의 변경을 뒤 실행이 덮어쓰는 것(lost update)을 막는다. 용어집·참고문헌·표준 목록은 이미 퍼블리셔가 현재 상태에서 합치므로 대상이 pages[] 로 좁다 |
| D-020 | 2026-09-25 | 3부 순서 | 3부 1(새 트랙)·2(시드 세부영역)·4(첫 트랙 백로그)는 서로 입력이 겹치지 않아 한 배치에서 동시에 돌리고(트랙마다 직렬), 3(대분류 연결)은 2가 끝난 뒤, 5(주간·월간)와 6(용어집·참고문헌·전체 검사)은 마지막에 차례로 돌린다 | "순서대로"의 목적은 앞 단계 결과를 뒤 단계가 쓰는 것이며, 의존이 없는 단계를 직렬로 두면 수십 시간이 걸린다 [가정] |
| D-021 | 2026-09-25 | 1-7 사용자 입력 | 실험 결과 반영 검증(V3)은 작업 트리 사본(git worktree)에서 커밋 없이 돌리고 증거만 runs/validation/ 으로 옮긴다. 정정 요청 검증(V2)은 본 위키에서 돌려 반영 1건은 그대로 두고, 입력함(inbox/corrections.md)만 원상 복구한다 | 예시 실험 결과는 가짜 데이터라 위키에 남기면 안 되고, 본 위키에서 되돌리면 동시에 게시되는 다른 실행과 섞인다. 정정 반영 1건은 실제로 맞는 수정이라 남긴다 |
| D-022 | 2026-09-25 | 1-5 차등 갱신 | 스토리텔러 R-3(patches)을 update·category_link 만이 아니라 기존 페이지를 고치는 모든 실행으로 넓혔다(스토리텔러 1.7). 예외는 영역 심화의 seed 세부영역 페이지 | 검증 실행 1(주제 조사)에서 세부영역 페이지 전체를 다시 출력했다. 출력 토큰이 비용의 큰 몫이다 |
| D-023 | 2026-09-25 | 1-3 자동 분리 | 분리 뒤 원 절에 남기는 요약은 outline summary 가 아니라 그 절 첫 문단의 앞 문장(최대 두 문장, 300자)이다. outline summary 는 예산 계획용으로만 쓴다 | 검증 실행 1의 2차 검증이 outline summary 의 편집 설명 문장("…를 더하고 연결한다")이 독자 본문에 실렸다고 지적했다. 앞 문장은 이미 쓴 독자용 본문이다 |
| D-024 | 2026-09-25 | 1-5 코드로 옮김 | 패치를 적용한 페이지의 version(+1)·updated(실행 날짜)·status(draft)와, 참조만 있고 정의가 없는 각주 정의 줄은 코드(validate.complete_patched_page)가 채운다 | 판단이 필요 없는 기계적 작업이며, 빠뜨리면 2차 검증이나 형식 검증에서 재작성으로 돌아온다 |
| D-025 | 2026-09-25 | 운영 안정성 | run_daily.sh 본문 전체를 중괄호 묶음 하나로 감싸 bash 가 실행 전에 끝까지 읽게 했고, 실행 중인 스크립트는 제자리 덮어쓰기 대신 새 파일로 바꿔 넣는다(mv) | 배치가 도는 동안 스크립트를 제자리에서 고치자 검증 실행 1이 2차 검증 뒤 "syntax error near unexpected token" 으로 끝났다(bash 는 스크립트를 명령 단위로 읽는다). 산출물은 그대로라 --resume --step publish 로 게시했다 |
| D-026 | 2026-09-25 | 1-2 형식 검증 | 형식 검증의 퍼블리셔 사전 검사(--precheck)에서도 참고문헌 id 재배정을 먼저 한다 | 병렬 실행이 같은 '다음 id'를 받은 충돌은 코드가 푸는 일인데, 사전 검사가 재배정 전에 검사해 스토리텔러 형식 수정(첫 트랙 3 실행, $2.37)으로 돌려보냈다 |
| D-027 | 2026-09-25 | 1-6 정정 요청 | 검증 산출물의 corrections_applied·corrections_rejected 에서 이번 실행 대상(target.json corrections)이 아닌 id 는 코드가 빼고, 퍼블리셔도 대상 요청만 반영·거절한다 | 정정 요청함이 열려 있던 동안 돈 트랙 실행의 검증 에이전트가 대상이 아닌 corr-002 를 거절 목록에 넣었다. 정리는 판단이 필요 없는 일이다 |
| D-028 | 2026-09-25 | 검사 | 트랙 시드 상태를 고정으로 검사하던 단위 테스트 2개(아이디어 페이지 status seed, 새 트랙 단계별 질문 3~5개)를 시작 질문·허용 상태 기준으로 바꿨다 | 실제 실행이 아이디어 페이지를 게시하고 후속 질문을 더하는 것은 정상 운영이다. 시작 질문 3~5개 규칙은 제기 근거 '사용자' 항목으로 계속 검사한다 |
| D-029 | 2026-09-25 | 3부 배치 | 배치 실행기에 두 가지를 더했다: 퍼블리셔가 실패하면 게시만 한 번 다시 하고(그래도 실패하면 항목을 새로 실행), plan 항목의 resume 키로 이미 만든 실행을 넘겨받아 게시한다 | 일시적 게시 실패 때문에 에이전트 단계(약 $10·20분)를 다시 돌리는 낭비를 막는다. 배치를 다시 띄울 때 진행 중이던 실행을 버리지 않는다 |
| D-030 | 2026-09-25 | 3부 배치 | 테스트는 실제 docs/ 에 쓰지 않는다(분리 이름 충돌 테스트를 임시 폴더로) | 배치 퍼블리셔가 docs 를 검사하는 중에 테스트가 만든 임시 파일이 사라져 세부영역 1 게시가 실패했다 |
| D-031 | 2026-09-25 | 1-2 사후 보정 | 퍼블리셔가 용어집 한 줄 정의를 바꿀 때 참조가 사라진 각주 정의를 뺀다 | 옛 정의의 각주(ref-002·cand-04)가 남아 mkdocs 엄격 빌드가 'footnote never referenced' 로 실패했다(세부영역 1 실행 2026-09-25-08). 판단이 필요 없는 정리다 |
| D-032 | 2026-09-25 | 1-5 비용 | 2차 검증 입력에서 pages.json 의 본문(content·patches)을 자리 표시로 바꾸고(완성 페이지는 pages/ 로 따로 준다) 템플릿 전문을 뺐다. 형식 검증 결과(format_check.md)를 대신 준다 | 같은 본문이 두 번 들어가 2차 검증 프롬프트가 49만 자였다(트랙 3 첫 실행). 같은 실행으로 다시 만들면 31만 자(−38%). 절 구성 검사는 코드가 한다(검증 에이전트 부록 R-3) |
| D-033 | 2026-09-25 | 3부 배치 | 리서치 에이전트를 부를 때 실행마다 참고문헌 id 구간(30개)을 잠금 아래 예약해 주고(runs/.cache/ref_id_blocks.json), 퍼블리셔의 id 재배정은 다른 실행이 예약한 구간 위로만 옮긴다. 쓰지 않은 번호는 빈 번호로 남긴다 | 병렬 실행이 같은 '다음 id'를 받아 게시 직전에 번호가 바뀌고 그 사이 다른 실행이 먼저 게시하자, 2차 검증이 본문의 id 를 다른 출처와 대조해 재작성을 반복하다 트랙 1 첫 실행(2026-09-25-06)이 보류됐다. 번호 연속성보다 id 안정성이 중요하다 [가정] |
| D-034 | 2026-09-25 | 1-5 비용 | 리서치·1차 검증 입력의 '최근 실행 research.md'(6.1)를 대상이 같은 실행(같은 세부영역·트랙·대분류) 최대 7회 + 그 밖의 최근 실행 2회로 좁혔다 | 배치로 여러 영역을 동시에 돌리면 최근 7회가 모두 다른 대상이라 입력만 약 5만 자 늘고 중복 방지에 쓸모가 적다. 다른 대상의 최근 2회는 교차 중복 확인용으로 남긴다 [가정] |
| D-035 | 2026-09-25 | 3부 배치 | D-033 보완: 게시·보류된 실행의 예약 구간은 풀고, 게시 직전(검증이 끝난 뒤)에 이번 실행의 새 출처 id 를 다른 실행의 예약 구간을 피한 가장 낮은 빈 번호로 옮긴다(번호 순서 유지). 참고문헌 id 형식은 세 자리 이상(^ref-\\d{3,}$)으로 넓혔다 | 예약 구간만 쓰면 실행마다 30개씩 번호가 커져 배치 끝에 ref-999 를 넘고 스키마(^ref-\\d{3}$)를 어긴다. 검증 중인 실행이 대조하는 번호는 바꾸지 않으면서 빈 번호를 메운다 |
| D-036 | 2026-09-25 | 3부 1 새 트랙 | 트랙 2·3 의 남은 실행은 자동 질문 선택 대신 시작 질문(제기 근거 '사용자')을 단계 순서대로 하나씩 지정해 돌린다(--stage N --question-ids qN-xx). 트랙마다 실행 수는 시작 질문 수(18)와 같다. 단계 전환은 지금처럼 검증 에이전트가 승인할 때만 기록된다 | 요청은 '백로그 질문 수만큼 주제 조사를 돌려 본문과 초안 문서를 만든다'이다. 자동 선택은 매 실행 새로 생기는 같은 단계 후속 질문을 먼저 골라, 첫 세 실행 만에 단계 1 질문이 4개에서 6~8개로 늘었고 18회를 돌아도 단계 2~5 페이지가 비게 된다. 후속 질문은 백로그에 남아 이후 정규 실행이 다룬다 [가정] |
| D-101 | 2026-09-25 | 운영: 설정 위치 | 알림·배포·cron 설정을 새 파일 config/ops.yaml 에 두고, settings.yaml 의 notify("none", 사양서 0장 원문 값)는 그대로 둔 채 실제 채널은 ops.yaml notify.channel 을 따른다(--status 가 두 값이 다르면 알려 준다) | settings.yaml 은 다른 작업자가 고치는 파일이고, 0장 원문 값을 바꾸지 않으면서 한 곳에서 스위치를 관리하기 위해서다 |
| D-102 | 2026-09-25 | 운영: 설정 오류 | ops.yaml 이 없거나 YAML 오류면 DEFAULTS(외부 스위치 모두 false)로 처리하고 _error 를 남긴다 | 설정 실수가 발송·배포·cron 을 켜는 쪽으로 흐르지 않게 한다 |
| D-103 | 2026-09-25 | 운영: 설정 읽기 경로 | ops.yaml 읽기(--get), 스위치 전환(--enable/--disable), 현황(--status) CLI 를 pipeline/lib/notify.py 에 둔다 | 셸 스크립트·cron 래퍼·GitHub 워크플로가 같은 읽기 코드를 쓰게 하고, 새 파일을 소유 범위 밖에 만들지 않기 위해서다 |
| D-104 | 2026-09-25 | 운영: 스위치 전환 | --enable/--disable 은 해당 절의 enabled(·channel·method) 줄만 정규식으로 바꾸고 주석 열 위치까지 보존한다 | yaml.dump 로 다시 쓰면 설명 주석이 모두 사라진다. 한 줄 명령으로 켜고 끌 수 있어야 한다 |
| D-105 | 2026-09-25 | 운영: 비용 합계 | cost_usd → total_cost_usd → cost(수 또는 total_usd·total_cost_usd·usd·total) → costs 순으로 찾고 없으면 줄을 생략한다 | 지금 summary.json 에는 비용 필드가 없고(agent_runner 는 호출별 total_cost_usd 만 로그), 다른 작업자가 어떤 키로 넣을지 모른다 |
| D-106 | 2026-09-25 | 운영: 메시지 구성 | 요청 필드(실행 id, 유형, 대상, 종료 상태, 생성/갱신, 비용, 로그 경로)에 보류 사유와 site_url 이 있을 때의 사이트 링크만 더하고 소요 시간·커밋은 넣지 않는다 | 짧은 알림이 목적이고, 보류 사유는 사람이 바로 판단해야 하는 정보다 |
| D-107 | 2026-09-25 | 운영: 실행 유형 이름 | 유형 한국어 이름은 호출 시 lib/runs.py 의 RUN_TYPE_KO 를 읽고 실패할 때만 사본을 쓴다 | 다른 작업자가 실행 유형(category_link)을 추가하고 있어 사본은 어긋난다(테스트로 확인) |
| D-108 | 2026-09-25 | 운영: 비밀값 | 웹훅 URL·SMTP 계정·비밀번호는 환경변수(ROP_SLACK_WEBHOOK_URL, ROP_SMTP_USERNAME, ROP_SMTP_PASSWORD)에서만 읽고, 오류 문구에 들어간 URL·계정·비밀번호는 ***로 가린다 | 비밀값을 파일·로그·일일 로그(커밋됨)에 남기지 않기 위해서다 |
| D-109 | 2026-09-25 | 운영: 슬랙 보안 | 웹훅 URL 은 https:// 만 허용한다 | 평문 전송으로 웹훅 토큰이 새는 것을 막는다 |
| D-110 | 2026-09-25 | 운영: 켜졌는데 비밀값 없음 | enabled: true 인데 환경변수가 비었으면 skipped 가 아니라 failed 로 돌려준다 | 설정 누락이 조용히 묻히지 않고 로그에 실패로 보이게 한다 |
| D-111 | 2026-09-25 | 운영: 이메일 연결 | use_tls 는 STARTTLS(587), 465 용으로 use_ssl 을 더했다. 계정·비밀번호는 둘 다 있거나 둘 다 없어야 한다 | 두 방식이 흔하고, 한쪽만 설정된 상태는 거의 항상 실수다 |
| D-112 | 2026-09-25 | 운영: 채널 both | 한 채널이라도 실패하면 status failed, detail 에 채널별 결과를 모두 적는다 | 상태값을 요청한 네 가지로 유지하면서 부분 실패를 숨기지 않는다 |
| D-113 | 2026-09-25 | 운영: 예외 처리 | send() 는 모든 예외를 잡아 failed 로 돌려준다(망가진 summary 포함) | 알림 실패가 게시·커밋을 되돌리면 안 된다(publish.py 는 커밋 뒤 오류를 경고로만 처리) |
| D-114 | 2026-09-25 | 운영: 시험 발송 | --test 는 실행 id 앞에 [시험]을 붙이고, --mock 은 enabled 를 무시하고 자리표시 URL·주소로 모의 전송 내용만 출력한다. 실제 시험 발송은 --test(enabled 필요) 또는 --test --force | 실제 채널로 가도 시험 메시지임을 알 수 있고, 모의 실행은 어떤 설정에서도 네트워크를 쓰지 않는다 |
| D-115 | 2026-09-25 | 운영: 배포 방식 | deploy.method 기본값은 gh-deploy(서버가 gh-pages 푸시), GitHub Actions 템플릿은 대안(method: actions)으로 두고 둘 중 하나만 쓰게 한다 | Pages 배포 원천은 하나뿐이고, 요청한 deploy_site.sh --yes 흐름과 cron 서버 운영이 한 흐름으로 이어진다 |
| D-116 | 2026-09-25 | 운영: 워크플로 스위치 | 템플릿 워크플로는 ops.yaml 의 deploy.enabled=true 이고 method=actions 일 때만 configure/upload/deploy 단계를 실행하고, 아니면 빌드만 한다 | 켜고 끄는 스위치를 ops.yaml 한 곳에 모은다 |
| D-117 | 2026-09-25 | 운영: 워크플로 트리거 | 기본은 workflow_dispatch 만 두고 push 트리거(branches: [main], paths: rop-wiki/**)는 주석으로 둔다 | 복사만으로 자동 배포가 시작되지 않게 한다. 서버가 어떤 브랜치에 푸시할지 아직 정해지지 않았다 |
| D-118 | 2026-09-25 | 운영: 워크플로 위치 | 템플릿은 rop-wiki/deploy/github-pages.yml 에만 두고 .github/ 에는 아무것도 만들지 않았다 | .github/workflows/ 에 있으면 푸시 즉시 실제 원격 배포가 될 수 있다 |
| D-119 | 2026-09-25 | 운영: 액션 버전 | checkout@v4, setup-python@v5, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4 를 쓴다(README 에 복사 시 최신 주 버전으로 올려도 된다고 적음) | 공식 문서에 널리 쓰인 조합이며 서로 호환이 확인된 주 버전이다 |
| D-120 | 2026-09-25 | 운영: 배포 전 확인 | deploy_site.sh 는 로컬 빌드(settings.site_build_cmd)가 실패하면 배포하지 않고(exit 1), git 원격이 없으면 exit 4, 요청한 명령(gh-deploy --force --remote-branch gh-pages)은 --no-history 없이 그대로 쓴다 | 깨진 사이트를 올리지 않고, 요청 명령과 다르게 gh-pages 이력을 지우지 않는다 |
| D-121 | 2026-09-25 | 운영: 자동 배포 시점 | gh-deploy 방식의 매일 배포는 cron 래퍼가 정규 실행 성공(exit 0) 뒤 부른다(deploy.after_daily_run). 수동 run_daily.sh 실행은 배포하지 않는다 | publish.py 를 고치지 않고 운영(cron) 경로에서만 외부에 영향을 준다 |
| D-122 | 2026-09-25 | 운영: 겹침 방지 | crontab 줄에 flock -n -E 75 <위키>/runs/.lock 을 두고, 잠금 충돌(75)일 때만 두 번째 명령(cron_wrapper.sh --lock-busy)이 "건너뜀"을 cron.log 에 남긴다 | flock 은 프로세스가 죽으면 커널이 잠금을 풀어 오래된 잠금 파일 문제가 없고, 건너뛴 사실이 로그에 남아야 한다 |
| D-123 | 2026-09-25 | 운영: 래퍼 종료 코드 | cron_wrapper.sh 는 run_daily.sh 의 종료 코드를 그대로 돌려주고 뜻(0 완료, 2 보류·설정 오류, 3 준비 중단, 4 퍼블리셔 실패, 124 시간 초과)을 로그에 적는다 | -E 75 로 잠금 충돌을 구분하므로 실행 실패와 섞이지 않는다 |
| D-124 | 2026-09-25 | 운영: 로그 순환 | cron.log 가 5MB 를 넘으면 실행 시작 전에 cron.log.1 로 옮기는 1세대 순환을 쓴다(끝 5MB 잘라 남기기 대신) | 줄 중간이 잘리지 않고, 매 실행마다 큰 파일을 다시 쓰지 않는다. 최대 약 10MB 로 제한된다 |
| D-125 | 2026-09-25 | 운영: 실행 시간 상한 | 래퍼가 run_daily.sh 를 timeout --kill-after=300 14400(4시간, cron.max_runtime_sec)으로 실행한다 | 걸린 실행이 잠금을 계속 잡으면 다음 날부터 모든 실행이 건너뛰어진다. 정상 실행은 약 30분이고 에이전트 호출 상한이 30분이다 |
| D-126 | 2026-09-25 | 운영: CRON_TZ | 이 서버(Ubuntu 24.04, cron 3.0pl1)의 cron 바이너리에 CRON_TZ 문자열이 없어 무시된다고 판단하고, tz_mode auto 가 지원 여부를 보고 지원하지 않으면 서버 시간대로 환산해 등록한다(UTC 서버: 0 21 * * *) | 기존 등록 블록은 UTC 서버에서 06:00 UTC(=15:00 KST)에 돌았을 것이다. 일광 절약 시간이 있는 시간대면 경고한다 |
| D-127 | 2026-09-25 | 운영: CRON_TZ 줄 | server 모드에서는 CRON_TZ= 줄을 두지 않고 주석으로만 설명한다 | 환산 시각과 CRON_TZ 줄이 함께 있으면 읽는 사람이 오해하고, cronie 로 옮기면 이중 적용된다 |
| D-128 | 2026-09-25 | 운영: 블록 위치 | 등록 블록은 언제나 crontab 맨 끝에 두고 블록 뒤에 다른 항목을 두지 말라고 주석에 적는다 | CRON_TZ·PATH 줄은 그 뒤의 모든 줄에 적용된다 |
| D-129 | 2026-09-25 | 운영: PATH 줄 | command -v 로 claude·mkdocs·python3·git 의 디렉터리를 PATH 에 넣는다(git 추가) | 퍼블리셔 7단계 커밋이 git 을 쓴다 |
| D-130 | 2026-09-25 | 운영: flock 없음 | flock(util-linux)이 없으면 실제 등록을 거부한다(미리 보기는 가능) | 겹침 방지 없이 운영 cron 을 등록하지 않는다 |
| D-131 | 2026-09-25 | 운영: 등록 스위치 | cron.enabled 가 false 면 install_cron.sh 가 exit 3 으로 거부하고(--force 로 한 번 무시), --dry-run 은 언제나 되며 uninstall_cron.sh 는 스위치와 관계없이 해제한다 | 켜는 동작만 막고 미리 보기·끄는 동작은 막지 않는다 |
| D-132 | 2026-09-25 | 운영: 환경 파일 | ~/.config/rop-wiki/env 가 있으면 set -a 로 읽고, 권한이 600/400 이 아니면 경고만 하며, 로그에는 알려진 변수 이름의 설정 여부만 남긴다 | 비밀값 파일을 권장 권한으로 유도하되 실행을 막지 않고, 값이 cron.log 에 남지 않게 한다 |
| D-133 | 2026-09-25 | 운영: MAILTO | crontab 의 MAILTO 는 건드리지 않는다 | 다른 cron 항목에도 적용되는 전역 설정이다. 출력은 래퍼가 cron.log 로 모두 보낸다 |
| D-134 | 2026-09-25 | 운영: 조기 실패 알림 | 실행 id 가 생기기 전의 실패(설정 파일 오류 등)는 알림 대상에서 뺐다(cron.log 에만 남음) | 보류·준비 중단·퍼블리셔 실패는 publish.py --log-only 경로에서 알릴 수 있고, 래퍼에서 따로 알리면 중복 알림이 생긴다 |
| D-135 | 2026-09-25 | 운영: 테스트 범위 | ops.yaml 이 꺼져 있는지 강제하는 테스트는 두지 않고, 코드 기본값(DEFAULTS)이 꺼짐인지와 ops.yaml 이 읽히는지만 검사한다 | 사용자가 스위치를 켜면 검사 전체가 깨지면 안 된다 |
| D-136 | 2026-09-25 | 운영: 테스트 격리 | 셸 테스트는 가짜 crontab·mkdocs·git 과 ROP_OPS_CONFIG·ROP_CRON_LOG·ROP_ENV_FILE·ROP_RUN_DAILY 환경변수로 격리한다 | 실제 crontab·원격 저장소·네트워크·Claude 호출을 건드리지 않고 등록·해제·잠금·배포 경로를 끝까지 실행해 볼 수 있다 |
| D-137 | 2026-09-25 | 운영: 실제 crontab | 실제 crontab 으로 거부(exit 3)·--force 등록·교체·해제를 확인한 뒤, 작업 전에 있던 옛 rop-wiki 블록까지 해제해 crontab 을 비워 두었다 | 요청대로 rop-wiki 블록 없이 끝내야 하고, 옛 블록은 CRON_TZ 미지원 cron 에서 잘못된 시각으로 등록된 것이었다 |
| D-138 | 2026-09-25 | 운영: 추적 제외 파일 | runs/cron.log·cron.log.1 을 .gitignore 에 넣을 것을 메인 작업자에게 요청한다(runs/.lock 은 다른 작업자가 이미 추가) | .gitignore 는 이 작업의 소유 파일이 아니고, 퍼블리셔는 runs/<id>/ 만 커밋해 두 파일이 미추적으로 남는다 |
| D-139 | 2026-09-25 | 운영: 검사 스크립트 | test_ops.py 를 run_all.sh 에 넣는 일은 메인 작업자에게 맡긴다 | run_all.sh 는 이 작업의 소유 파일이 아니다 |
| D-201 | 2026-09-25 | 원문: 미러 탐색 | 저장소 경로는 `git clone --depth 1 --filter=blob:none --no-checkout` 으로 파일 목록만 받아 찾고, 등록 전 raw.githubusercontent.com 에서 실제로 받아 200·300바이트 이상인지 확인했다(생성 스크립트 scratchpad/wave1/gen_mirrors.py) | github.com 페이지·API 는 막혀 있지만 공개 저장소의 git 프로토콜은 열려 있어 추측 없이 정확한 경로를 얻을 수 있다 |
| D-202 | 2026-09-25 | 원문: 미러 목록 | config/source_mirrors.yaml 에 `relation`(original·official_artifact·related) 필드를 더했다 | 같은 문서의 원본과 같은 표준의 스키마·온톨로지, 제3자 구현을 구분하지 않으면 "원문 열람"이 과대 표시된다 |
| D-203 | 2026-09-25 | 원문: 미러 목록 | 확인하지 못한 정본 URL(IDTA 사이트의 템플릿 페이지 등)은 match 에 넣지 않고 GitHub 경로만 match 로 두었다 | 추측한 URL 을 목록에 두지 않는다는 규칙(확인한 것만 등록) |
| D-204 | 2026-09-25 | 원문: 미러 목록 | 300바이트 미만 파일 2개(admin-shell-io published/README.md, ARIAC_docs README.md)는 목록에서 뺐다 | 실제 내용이 거의 없어 원문 경로로 쓸 가치가 없다 |
| D-205 | 2026-09-25 | 원문: 미러 목록 | OpenEPCIS 문서(ref-013), EPC TDS(ref-021), Plattform I4.0(ref-035), 논문(arXiv·Nature·Frontiers·KCI)은 미러를 두지 않았다 | GitHub 공식 원문을 찾지 못했다(gs1/TDT 는 변환 규칙 저장소라 TDS 본문이 아니다) |
| D-206 | 2026-09-25 | 원문: 미러 판정 | EPCIS 온톨로지(ref-003), CBV(ref-014), VDA 5050 2.0.0 태그(ref-022), W3C SSN 편집자 초안(ref-030), MassRobotics 표준 저장소(ref-033), OPC UA 로보틱스 노드셋(ref-034)은 official_artifact 로 두고 원문 열람으로 치지 않았다 | 저장소 README 가 "초안"이라 하거나(EPCIS), 머리말에 'RELEASE CANDIDATE' 가 남아 있거나(VDA 2.0.0), 편집자 초안이라 게시판과 같다고 확인할 수 없다. 인용한 페이지 본문과 같은 글이 아니다 |
| D-207 | 2026-09-25 | 원문: 미러 판정 | IEEE 1872.2 OWL(hsu-aut), KnowRob·SOMA OWL, VDA 5050 3.0.0 태그(보도자료 ref-032)는 related 로 두었다 | 제3자 구현이거나 논문·보도자료가 설명하는 대상일 뿐 인용 문서 자체가 아니다 |
| D-208 | 2026-09-25 | 원문: 미러 판정 | original 항목이 URL 접두어로만 맞으면(같은 사이트의 다른 페이지) related 로 낮춘다 | 책 목차(SUMMARY.md)나 문서 첫 페이지(index.rst)를 다른 장의 원문으로 잘못 저장하는 것을 막는다 |
| D-209 | 2026-09-25 | 원문: 미러 판정 | github.com/<owner>/<repo>/blob/<ref>/<path> 는 목록에 없어도 raw 경로로 바꿔 original 로 본다 | 같은 파일의 다른 표현이므로 확인이 필요 없다(ref-031) |
| D-210 | 2026-09-25 | 원문: 원문 열람 규칙 | fetched 는 "본문을 실제로 읽음"만 뜻한다. URL 이 열리는 것, 검색 스니펫, 공식 산출물·관련 파일을 연 것은 열람이 아니다 | 사용자 규칙(원문 미열람 출처는 medium 상한)을 코드로 강제하려면 기준이 좁고 분명해야 한다 |
| D-211 | 2026-09-25 | 원문: 원문 열람 규칙 | 에이전트의 webfetch 표시는 web_fetch_available: false 실행에서 받아들이지 않고, github_raw 표시는 fetch_url 이 raw 경로이면서 official_artifact·related 미러가 아닐 때(또는 original 미러가 있을 때)만 받아들인다 | 이 환경은 WebFetch 점검이 실패해도 raw.githubusercontent.com 은 열린다(D-002). 반대로 점검 실패 실행에서 일반 사이트를 열었다는 표시는 모순이다 |
| D-212 | 2026-09-25 | 원문: 원문 열람 규칙 | data/source_texts 에 원문 텍스트가 있으면 에이전트 표시가 없어도 fetched true(경로는 텍스트의 fetched_via) | 텍스트가 프롬프트 입력으로 들어가므로 원문을 읽은 것과 같다 |
| D-213 | 2026-09-25 | 원문: 원문 열람 규칙 | apply_fetch_caps 는 출처의 source_unopened 를 fetched 의 반대로 맞추고, 발견 사항의 source_unopened 도 근거 출처가 모두 미열람일 때 true 로 맞춘다 | 퍼블리셔 _apply_references 가 source_unopened 로 "원문 열람" 칸과 각주의 "(원문 미열람)"을 정하므로 두 값이 어긋나면 안 된다 |
| D-214 | 2026-09-25 | 원문: 원문 열람 규칙 | 같은 출처 id 를 두 번 적은 발견 사항은 출처 1개로 센다 | high 는 서로 다른 출처 2개 이상이라는 규칙 |
| D-215 | 2026-09-25 | 원문: URL 확인 | 정책 차단 판정: CONNECT 403/407("Tunnel connection failed"), 평문 HTTP 403 + x-deny-reason 헤더, 본문 "Host not in allowlist"·"not enabled for this session". 그 밖의 403 은 오류 | 실제 응답을 확인했다: 평문 HTTP 는 투명 프록시가 403+x-deny-reason, github.com 은 403 JSON, 그 밖의 HTTPS 는 CONNECT 403. 사이트 자체의 403(봇 차단)과 섞지 않는다 |
| D-216 | 2026-09-25 | 원문: URL 확인 | HEAD 가 정책 차단이 아닌 403·405·501 이면 GET 으로 다시 확인하고, 정책 차단이면 다시 요청하지 않는다 | HEAD 를 거부하는 사이트가 있고, github.com 은 GET 본문으로만 정책 거부를 알 수 있다. 프록시 정책 거부는 재시도하지 않는다(프록시 안내) |
| D-217 | 2026-09-25 | 원문: URL 확인 | 정본이 열리지 않으면(정책 차단뿐 아니라 오류·없음도) 미러를 확인하고, 정본이 열리면 미러는 기록만 하고 요청하지 않는다 | 원문 경로가 살아 있는지 알려 주되 불필요한 요청은 줄인다 |
| D-218 | 2026-09-25 | 원문: URL 확인 | data/url_check.json 을 기본 출력으로 하고 `--refs` 로 일부만 확인하면 기존 결과에 합친다 | 사양이 data/url_check.json 을 요구했고, 부분 확인이 다른 참고문헌 결과를 지우면 안 된다 |
| D-219 | 2026-09-25 | 원문: URL 확인 | 표준 출력 마지막 줄 형식 `[check_urls] N건: …` 은 유지했다 | run_daily.sh weekly_checks 가 이 줄을 grep 한다 |
| D-220 | 2026-09-25 | 원문: URL 확인 | 확인 시각은 Asia/Seoul 로 기록한다 | 위키의 날짜 기준 시간대(settings.timezone) |
| D-221 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문 열람 상태 블록을 비고 절 끝에 `<!-- source-fetch:start/end -->` 마커로 둔다(auto: 접두어 아님) | autoregion.AUTO_KEYS 는 다른 담당이 고치는 중이고, 등록되지 않은 auto 키는 퍼블리셔 strict 갱신을 실패시킨다. 필요하면 메인이 auto 키로 등록할 수 있다 |
| D-222 | 2026-09-25 | 원문: 참고문헌 페이지 | 시드 10건을 5개 절(서지 정보·요약·인용된 페이지·각주 형식·비고)로 바꾸면서 "원문 열람:" 문단은 비고로 옮기고, 요약 끝에 "등록: 구축 시드(분류 원문 12장 참고 자료 N번, 2026-09-24) · 검증: 시드 상태(검증 실행 전)" 줄을 더했다 | 퍼블리셔 형식의 "등록 실행 · 검증" 줄에 대응하는 사실만 적었다. [분류원문] 줄·표·auto 영역·각주 줄은 글자 그대로 둔다 |
| D-223 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문을 열람하게 된 페이지의 "원문 미열람" 비고 문단은 지우지 않고 "등록 당시 기록: … → YYYY-MM-DD 에 <경로> 경로로 원문을 열람했다"로 바꾼다 | 기존 사실을 지우지 않으면서 현재 상태와 모순되지 않게 한다 |
| D-224 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문 미열람 출처의 각주 형식 줄에 " (원문 미열람)"을 붙였다(시드 10건은 없었다). 열람한 출처는 떼고 접근일을 수집일로 바꾼다 | 공통 규칙 6절·템플릿이 요구하는 표기이며 스토리텔러가 이 줄을 그대로 복사한다 |
| D-225 | 2026-09-25 | 원문: 참고문헌 페이지 | 원문을 열람(fetched)한 출처만 신뢰도 medium 상한을 풀고 유형 기준값(표준·논문·정부·연구기관·오픈소스 문서 high)으로 올린다. URL 이 열리는 것만으로는 올리지 않는다(기존 --apply-url-check 동작 변경) | 사용자 규칙 "fetched false 출처는 medium 상한"과 같은 기준. URL 열림은 실재 확인일 뿐 내용 확인이 아니다 |
| D-226 | 2026-09-25 | 원문: 참고문헌 페이지 | 이번에 원문 열람(github_raw)으로 표시한 것은 relation original 이 확인된 8건(ref-004, 008, 009, 010, 023, 031, 039, 040)뿐이다. 나머지 35건은 fetched: false | curl 로 200 과 본문을 확인하고 ingest 로 data/source_texts 에 저장한 것만 열람으로 인정했다 |
| D-227 | 2026-09-25 | 원문: 참고문헌 페이지 | 프런트매터에 fetched·fetched_via·url_status·url_checked(·열람 시 source_text, reliability_by_type)를 더했고 바뀐 페이지는 updated=2026-09-25, version +1 | 기존 apply_url_check 의 갱신 관례(updated·version)를 따랐다 |
| D-228 | 2026-09-25 | 원문: 참고문헌 페이지 | 미열람 행에 이미 붙어 있던 설명("(구축 환경의 네트워크 정책으로 URL 을 열지 못함)")은 그대로 둔다 | 기존 사실 보존 |
| D-229 | 2026-09-25 | 원문: 파일 소유 | 이번 작업의 반영(ingest --mirrors, --apply-url-check)은 data/changelog.json 에 쓰지 않았다(--no-changelog). 기능 자체는 기본값으로 변경 이력을 남긴다 | data/changelog.json 은 다른 담당도 고치는 공용 파일이라 동시 수정 충돌을 피했다. 운영에서는 변경 이력이 있어야 추적할 수 있다 |
| D-230 | 2026-09-25 | 원문: 파일 소유 | scaffold.py main() 대신 apply_url_check() 를 직접 불러 반영했다 | main() 은 끝에 auto 영역 전체와 mkdocs.yml 을 다시 쓰는데, 이는 다른 담당이 고치는 중인 파일이다 |
| D-231 | 2026-09-25 | 원문: 수집(inbox) | manifest 항목에 file 필드를 필수로 두고, 처리 뒤 manifest 는 고치지 않는다(파일 이동 + sha256 비교로 멱등) | 사람이 쓴 YAML(주석 포함)을 스크립트가 다시 쓰지 않는다 |
| D-232 | 2026-09-25 | 원문: 수집(inbox) | 참고문헌 연결 우선순위: ref_id → URL 정규형 일치(scheme·www·끝 슬래시·#조각 무시) → 없으면 src-<slug> 로 저장만 | 사람이 명시한 값이 우선이고, 참고문헌이 나중에 생기면 다음 실행의 sync 가 연결한다 |
| D-233 | 2026-09-25 | 원문: 수집(inbox) | 추출 텍스트가 20자 미만이면 실패로 보고 파일을 inbox 에 남긴다 | 스캔 이미지 PDF 를 "원문 열람"으로 잘못 표시하지 않는다 |
| D-234 | 2026-09-25 | 원문: PDF | poppler-utils 를 apt-get 으로 설치했다(apt-get update 뒤 성공). pypdf 는 별도 프로세스에서 cryptography 를 막고 불러 쓰는 대체 경로로 둔다 | 시스템 cryptography 가 import 중 pyo3 패닉(BaseException)을 내 같은 프로세스에서는 잡을 수 없다. 별도 프로세스면 패닉이 격리된다 |
| D-235 | 2026-09-25 | 원문: 텍스트 저장 | 원문 텍스트는 가공 없이(마크다운·rst 그대로, HTML 만 텍스트로) 저장하고 메타데이터는 index.json 에만 둔다 | sha256 로 변경을 판정하고 원문을 그대로 보존한다 |
| D-236 | 2026-09-25 | 원문: 프롬프트 입력 | select_texts_for_prompt 는 인용 출처는 앞부분, 키워드 일치 텍스트는 키워드 주변(±400자) 발췌로, 남은 예산을 남은 항목 수로 나눠 준다(최소 1500자) | 인용 출처가 먼저라는 사양과 프롬프트 크기(비용, D-004) 제한을 함께 지킨다 |
| D-237 | 2026-09-25 | 원문: 테스트 | 테스트는 임시 폴더(TemporaryDirectory)와 가짜 urlopen 만 쓰고, 저장소 안의 실제 미러 파일은 읽기만 한다. PDF 테스트는 도구가 없으면 건너뛴다 | 저장소에 파일을 남기지 않고 네트워크에 의존하지 않는다 |
| D-301 | 2026-09-25 | 트랙·아이디어: 아이디어 1 위치 | 새 트랙을 만들지 않고 기존 트랙 manual-capability-ontology 를 확장했다(연구 목표 4 "작업 할당 질의", 5 "신규 로봇 온보딩 시 능력 정의 초안 자동 생성" 추가, 트랙 정의 research_goals·scope_capabilities 추가) | 사용자 요청 원문이 기존 트랙 확장을 지시한다 |
| D-302 | 2026-09-25 | 트랙·아이디어: 아이디어 1 범위 능력 | 이동·계단·적재·도어 조작·충전을 트랙 정의 scope_capabilities 와 개요 컨셉 절에만 적고 능력 온톨로지 초안에는 넣지 않았다 | 초안 변경에는 근거 finding id 가 필요하다(트랙 출처 규칙). 조사 순서만 정한다 |
| D-303 | 2026-09-25 | 트랙·아이디어: 아이디어 1 백로그 중복 | 요청 질문 "로봇 능력을 표현하는 기존 온톨로지·표준은 무엇이 있는가"는 새 id 없이 q1-01 에 대응시켰다 | q1-01(로봇 능력·작업을 표현하는 온톨로지·지식 모델에는 무엇이 있고…)과 같은 질문이며 이미 실행 2026-09-25-02에서 답함 |
| D-304 | 2026-09-25 | 트랙·아이디어: 아이디어 1 백로그 | 요청 질문 2개를 q3-07(추출 오류 유형, 단계 3)·q4-07("이 작업을 할 수 있는 로봇" 질의 형식, 단계 4)로 문구 그대로(끝 물음표만 더함) 등록 | 추출 정확도·한계는 단계 3의 밝힐 것, 질의 형식은 능력을 배정·실행에 잇는 단계 4의 밝힐 것이다. q5-02(오류 비용·탐지)와는 묻는 축(빈도)이 달라 따로 둔다 |
| D-305 | 2026-09-25 | 트랙·아이디어: 아이디어 1 백로그 | 새 목표 질문 4개 추가: q4-08(정적 능력과 동적 상태 결합, 목표 4), q5-06(작업 할당 질의 시험, 목표 4), q3-08(능력 정의 초안 자동 생성 형식, 목표 5), q7-02(온보딩 초안 효과 측정, 목표 5) | 목표 4는 설계(단계 4)와 검증(단계 5), 목표 5는 추출(단계 3)과 시나리오·가설 3 판정(단계 7)에 걸린다. id 는 단계별 다음 빈 번호 |
| D-306 | 2026-09-25 | 트랙·아이디어: 아이디어 1 백로그 | 새 항목의 키는 기존 시드와 같은 8개(id·question·stage·origin·status·answered_run_id·answer_link·created), origin 사용자, created 2026-09-25 | backlog.json 스키마를 바꾸지 않는다 |
| D-307 | 2026-09-25 | 트랙·아이디어: 기존 트랙 단계 페이지 | 단계 3·4·5·7 질문 표에 새 질문 행을 더하고, 백로그에만 있던 q4-06·q5-05(실행 2026-09-25-02의 후속 질문)도 표에 옮겼다. 단계 상태 줄의 열린 질문 수를 백로그와 맞췄다 | 단계 페이지 표는 백로그와 일치시키는 것이 규칙이다 |
| D-308 | 2026-09-25 | 트랙·아이디어: 기존 트랙 관련 영역 | 트랙 정의 related_areas 에 10. 설비·건물 시스템 연동, 16. 공용 자원·충전·에너지 최적화를 더하고 개요 4절에 네 번째 묶음으로 적었다 | 아이디어 1의 범위 능력 "도어 조작"(문·승강기 연동 경계)과 "충전"(충전기 배분)이 두 영역에 직접 걸린다. 연결 추가이며 분류 변경이 아니다 |
| D-309 | 2026-09-25 | 트랙·아이디어: 트랙 로그 | 세 트랙 log.json 에 run_id build-2026-09-25 항목을 더했다(기존 트랙은 편입 기록, 새 트랙은 시드 기록) | 구축 시드를 build-2026-09-24 로 기록한 선례와 같게 추적 가능하게 한다. 파이프라인 실행이 아니므로 일일 로그는 없다 |
| D-310 | 2026-09-25 | 트랙·아이디어: 새 트랙 slug·이름 | nl-task-chatbot "자연어 업무 지시 챗봇", floorplan-recognition "건축 도면 자동 인식" | 요청된 slug·이름 그대로 |
| D-311 | 2026-09-25 | 트랙·아이디어: 새 트랙 단계 | 5단계: 1 선행 연구·제품 사례 조사 / 2 필요한 데이터와 표준 조사 / 3 구현 가설 설계 / 4 오해석 방지와 확인 절차(챗봇)·지도 변환 보정과 현장 정합(도면) / 5 검증 방법과 가설 판정 | 요청의 제안 단계를 그대로 썼다. 아이디어 페이지 3~6절(사례·데이터와 표준·구현 가설·검증)과 단계가 1:1로 대응해 트랙 실행 결과가 들어갈 자리가 분명하다 |
| D-312 | 2026-09-25 | 트랙·아이디어: 새 트랙 primary_area | 챗봇 13. 작업 배정 — MRTA, 도면 6. 지도·공간·위치 모델 | select_target 이 트랙 실행의 대상 영역으로 primary_area 를 쓴다. 도면은 분류 원문 10장의 중심 영역, 챗봇은 정의의 핵심 동사(적합한 로봇을 찾아 배정)가 13번의 문제다 |
| D-313 | 2026-09-25 | 트랙·아이디어: 새 트랙 시작 백로그 | 단계마다 3~4개, 트랙당 18개(요청 질문 3개 + 구축자 시작 질문 15개), 모두 origin 사용자·상태 열림 | 요청이 단계별 3~5개 시작 질문을 요구한다. 요청 질문은 문구 그대로 두고 끝에 물음표만 더했다 |
| D-314 | 2026-09-25 | 트랙·아이디어: 새 트랙 요청 질문 배치 | 챗봇: 분해 접근 → q1-01(단계 1), 스케줄링 LLM/최적화 → q3-01(단계 3), 확인 절차 → q4-01(단계 4) / 도면: 데이터셋·모델 → q1-01(단계 1), 공간 그래프 표준 → q2-01(단계 2), 지도 보정 → q4-01(단계 4) | 각 질문이 묻는 것이 그 단계의 밝힐 것과 같다 |
| D-315 | 2026-09-25 | 트랙·아이디어: 시작 질문의 출처 후보 | 새 트랙 질문에는 괄호 속 출처 후보 이름을 넣지 않았다(요청에 있는 BIM·IFC, 실내 공간 표준만 유지) | 사실을 지어내지 않는다. 후보 실재 확인은 리서치 에이전트의 일이다 |
| D-316 | 2026-09-25 | 트랙·아이디어: 가설 | 새 트랙마다 [가설] 3개(미판정, 단계 5에서 판정)를 아이디어 정의에서 도출했다 | 트랙 개요 템플릿이 가설과 판정 상태를 요구한다. 도출이므로 [가정]을 병기 |
| D-317 | 2026-09-25 | 트랙·아이디어: 초안 문서 | 챗봇 task-model-draft.md "업무 분해·배정 설계 초안", 도면 space-graph-schema-draft.md "공간 그래프 스키마 초안". type 은 ontology-draft 를 그대로 쓰고 v0 개념·관계는 아이디어 정의 문구에서만 도출(모두 "아이디어 정의 기반 [가정]") | 퍼블리셔·검사·에이전트가 트랙 초안을 같은 유형으로 다룬다. 요청 문구 밖의 개념은 근거 finding 없이 넣지 않는다 |
| D-318 | 2026-09-25 | 트랙·아이디어: 초안 버전 키 | 새 초안도 프런트매터 키 ontology_version 을 쓰고, 버전 이력 auto key 도 ontology-version-history 를 그대로 쓴다. 버전 원천 파일은 트랙 정의 draft_versions(task_model_versions.json, space_graph_versions.json) | pages.json 의 track_updates.ontology_draft_version 대조와 호환. 키 이름만 공유하고 파일은 트랙별로 둔다 |
| D-319 | 2026-09-25 | 트랙·아이디어: 초안 일반화 | 트랙 정의에 draft_page·draft_title·draft_template·draft_versions·draft_version_label 을 두고 paths.track_draft_page/track_draft_rel/track_draft_versions/track_page_order 로 읽는다. 첫 트랙은 기본값과 같은 값을 선언해 출력이 바뀌지 않는다 | TRACK_PAGE_ORDER·render 의 ontology_versions.json 하드코딩을 트랙마다 선언으로 바꾼다(코디네이터 요청 키와 같게) |
| D-320 | 2026-09-25 | 트랙·아이디어: 초안 템플릿 | 새 트랙용 일반 템플릿 templates/track-draft.md 를 만들었다(draft_template: track-draft.md). 첫 트랙은 ontology-draft.md 유지 | 온톨로지 초안 템플릿의 안내가 능력 온톨로지 전용이라 새 초안에 맞지 않는다. 절 구성은 같게 두었다 |
| D-321 | 2026-09-25 | 트랙·아이디어: stage_artifacts | 첫 트랙 {1: 비교표, 2: 매트릭스, 3: 실험, 5: 평가 절차·실험, 7: 실험}, 새 트랙 {3: 실험, 5: 실험} | 코디네이터가 준 첫 트랙 값 그대로. 새 트랙은 사양서 8.2의 단계 3·5·7 실험 제안 규칙을 5단계에 맞춰 3·5로 줄였다 |
| D-322 | 2026-09-25 | 트랙·아이디어: runs_per_week | 세 트랙 모두 null(비우면 settings.track_runs_per_week 를 따르고 트랙 사이 배분은 settings.track_weights) | 코디네이터 요청. 트랙별 값이 있으면 select_target 이 최댓값을 써 설정을 덮는다 |
| D-323 | 2026-09-25 | 트랙·아이디어: 트랙 순서 | 트랙 정의에 order(1 첫 트랙, 2 챗봇, 3 도면)를 두고 paths.ordered_track_slugs 가 order 순으로 정렬한다. 홈 트랙 현황·내비·매핑표·열린 질문이 같은 순서를 쓴다 | 기존 트랙이 맨 앞이어야 하고, slug 알파벳순(floorplan 이 먼저)은 요구와 다르다 |
| D-324 | 2026-09-25 | 트랙·아이디어: glossary_targets | 세 트랙에 8~9개씩 핵심 용어 목록을 두었다(용어집 항목 작성은 하지 않음) | 용어집 항목의 정의·출처는 에이전트가 근거와 함께 쓴다. 목록은 조사 대상일 뿐이다 |
| D-325 | 2026-09-25 | 트랙·아이디어: idea_page 형식 | docs/ideas/<slug>.md 형식(저장소 루트 기준)으로 적고 paths.idea_page_rel 이 docs 기준 경로로 바꾼다 | agent_runner 가 이 형식을 읽는다(코디네이터 안내) |
| D-326 | 2026-09-25 | 트랙·아이디어: 아이디어 페이지 | type idea(frontmatter.py TYPE_VALUES·TYPE_REQUIRED track 추가, 색인은 subtype index 로 면제), status seed, 7절 구성은 요청 그대로. 3~6절은 "아직 조사되지 않음 — <트랙> 트랙 실행이 채운다"와 단계 링크만 | 사실을 지어내지 않는다. 아이디어 1은 이미 단계 1 결과가 있어 "아직 이 페이지로 정리되지 않음"으로 쓰고 결과가 쌓이는 페이지를 링크했다 |
| D-327 | 2026-09-25 | 트랙·아이디어: 아이디어 페이지 1절 | 정의 문구 원문 인용 + 가장 가까운 세부영역의 SCM 관점 질문([분류원문] 인용) + 현장 문제 한 단락([가정]) | 현장 문제를 원문 질문에 묶어 근거 없는 서술을 줄인다 |
| D-328 | 2026-09-25 | 트랙·아이디어: 아이디어 페이지 2·7절 | auto:idea-areas(트랙 정의 idea_areas·idea_area_notes)와 auto:idea-backlog(백로그를 열림 → 조사 중 → 답함 → 보류 → 폐기 순) 영역으로 만든다 | 매핑과 백로그의 단일 원천을 유지한다 |
| D-329 | 2026-09-25 | 트랙·아이디어: 매핑 표기 | ● 중심 영역(idea_areas.primary), ○ 함께 필요한 영역(idea_areas.related), 빈칸 연결 없음. 원천은 각 트랙 정의이며 매핑표(auto:idea-area-map)와 세부영역 "관련 연구 트랙"(auto:area-tracks)이 같은 원천에서 렌더링된다 | 매핑표와 세부영역 페이지가 어긋나지 않게 한다 |
| D-330 | 2026-09-25 | 트랙·아이디어: 매핑 규모 | 아이디어 1: ● 1·○ 12(13개 영역), 아이디어 2: ● 4·○ 12(16개), 아이디어 3: ● 1·○ 12(13개). 어느 아이디어와도 연결되지 않은 영역은 4. 성과·경제성·프로세스 개선, 7. 화물·재고·자산 식별과 추적, 11. 분산 시스템·통신·컴퓨팅 구조, 17. 로봇 간 협업·물리적 인계 | 아이디어 정의 문구와 분류 원문 10장·8장에 직접 걸리는 영역만 연결했다 |
| D-331 | 2026-09-25 | 트랙·아이디어: 매핑 원칙 | 분류 원문 10장 매핑(아이디어 1: 5·9·21·23·24, 아이디어 3: 6·15·21·22)과 8장 교차 규칙(27. AI·학습·적응과 모델 운영: 매뉴얼 해석 5·21, 도면 해석 6)은 그대로 따르고, 나머지는 정의 문구의 동사·명사가 직접 가리키는 영역만 구축자 제안으로 더했다 | 원문을 바꾸지 않고 연결만 더한다 |
| D-332 | 2026-09-25 | 트랙·아이디어: area-tracks 역할 | 세부영역이 트랙의 primary_area 이거나 매핑 ●이면 "중심 영역(●)", related_areas 이거나 ○이면 "함께 필요한 영역(○)" | 트랙 정의와 매핑의 합집합으로 링크를 만든다(요청) |
| D-333 | 2026-09-25 | 트랙·아이디어: area-tracks 위치 | 28개 세부영역 페이지의 소속 대분류 admonition 세 줄 바로 아래, 1절 앞에 auto:area-tracks 마커를 넣었다(연결 없는 4개 영역은 빈 영역). admonition·1·2절은 한 글자도 바꾸지 않았다 | protect_source 가 preamble 에서 H1·핵심 질문·대분류 이름의 포함만 보고 1·2절을 글자 단위로 대조하므로 통과한다 |
| D-334 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 5. 로봇 능력·작업 온톨로지 | ● | 분류 원문 10장이 매뉴얼 기반 로봇 온톨로지의 중심 연구영역으로 둔다. 능력·제약을 공통 모델로 표현하는 일 자체다 |
| D-335 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 8. 실시간 세계 상태·데이터 일관성 | ○ | 능력의 실행 조건(배터리·적재량·문 상태)을 실행 시점에 판단하는 쪽이다(기존 트랙의 활용처 연결) |
| D-336 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 9. 로봇·제조사 관제 연동 | ○ | 분류 원문 10장의 함께 필요한 영역(어댑터). SDK 문서의 능력 기술을 실제 명령·상태로 잇는다 |
| D-337 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 10. 설비·건물 시스템 연동 | ○ | 범위 능력 '도어 조작'. 로봇이 문·승강기를 직접 다루는지 설비 연동으로 여는지의 경계가 능력 정의에 들어간다 |
| D-338 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 12. 명령·작업 실행의 신뢰성 | ○ | 능력과 명령의 연결. 할당한 작업이 실행 단계에서 확실히 수행되는지와 이어진다(기존 트랙의 활용처 연결) |
| D-339 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 13. 작업 배정 — MRTA | ○ | 연구 목표 4 '작업 할당 질의'. 수행 가능한 로봇을 질의로 찾는 결과가 배정의 입력이다 |
| D-340 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 16. 공용 자원·충전·에너지 최적화 | ○ | 범위 능력 '충전'. 충전 능력·조건이 충전기 배분과 충전 시점 계획의 입력이 된다 |
| D-341 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 21. 온보딩·설정·현장 시운전 | ○ | 분류 원문 10장의 함께 필요한 영역(온보딩)이며 연구 목표 5 '능력 정의 초안 자동 생성'의 적용처다 |
| D-342 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 23. 시험·형식 검증·벤치마크 | ○ | 분류 원문 10장의 함께 필요한 영역(검증). 추출한 능력과 질의 결과를 시험으로 확인한다 |
| D-343 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 24. 자산·소프트웨어 수명주기 관리 | ○ | 분류 원문 10장의 함께 필요한 영역(버전 관리). 문서·펌웨어 개정에 따라 능력 정의가 바뀐다 |
| D-344 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 25. 안전·위험 관리 | ○ | 문서에 적힌 안전 제약을 능력의 제약으로 담는다(기존 트랙의 활용처 연결) |
| D-345 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 27. AI·학습·적응과 모델 운영 | ○ | 분류 원문 8장의 교차 규칙: 매뉴얼 해석은 이 영역의 방법이 5. 로봇 능력·작업 온톨로지와 21. 온보딩·설정·현장 시운전에 적용되는 것이다 |
| D-346 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 1. 로봇 기능 온톨로지 × 28. 표준·상호운용성·다사업자 거버넌스 | ○ | 능력 기술 표준과 제조사·ROP 사이의 책임(기존 트랙의 활용처 연결) |
| D-347 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 13. 작업 배정 — MRTA | ● | '온톨로지로 적합한 로봇을 찾아 배정'하는 일이 이 영역의 배정 문제다 |
| D-348 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 14. 작업 순서·스케줄링 | ● | '작업 진행과 스케줄링을 자동으로 관리'하는 일이 이 영역의 순서·시간 제약·긴급 작업 삽입 문제다 |
| D-349 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 18. 사람–로봇 협업·운영 인터페이스 | ● | 채팅은 작업자·관리자가 일을 지시하고 확인·승인하는 운영 인터페이스다 |
| D-350 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 27. AI·학습·적응과 모델 운영 | ● | 이 영역 정의의 LLM 에이전트와, AI가 만든 작업 계획을 실행에 쓰는 기준을 묻는 이 영역의 질문이 해석과 오해석 방지 단계에 그대로 걸린다 |
| D-351 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 1. 주문·업무 시스템 연계 | ○ | 채팅 지시는 업무 시스템의 주문·요청과 나란히 들어오는 업무 요청이므로 변경·취소·완료 반영 규칙을 함께 본다 |
| D-352 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 2. 공정·워크플로 모델링 | ○ | 분해 결과가 들어갈 작업 단계·선후관계·완료 조건의 틀을 이 영역이 정의한다 |
| D-353 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 5. 로봇 능력·작업 온톨로지 | ○ | '온톨로지로 적합한 로봇을 찾는' 질의의 대상이다(아이디어 1의 산출물) |
| D-354 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 6. 지도·공간·위치 모델 | ○ | 지시 속 장소 표현(예: 층·구역 이름)을 공간 노드로 해석한다(아이디어 3의 산출물) |
| D-355 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 8. 실시간 세계 상태·데이터 일관성 | ○ | 배정 시점의 로봇 위치·배터리·가용 상태를 현재 상태로 확인한다 |
| D-356 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 12. 명령·작업 실행의 신뢰성 | ○ | 배정 뒤 명령의 접수·실행·완료·취소 상태와 같은 지시의 중복 처리 방지가 필요하다 |
| D-357 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 16. 공용 자원·충전·에너지 최적화 | ○ | 배치할 때 승강기·충전기 같은 공용 자원 예약을 함께 정한다 |
| D-358 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 19. 모니터링·이상 탐지·원인 분석 | ○ | '작업 진행 관리'에서 지연·이상을 탐지하고 원인을 설명한다 |
| D-359 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 20. 예외 복구·재계획·업무 연속성 | ○ | 진행 중 고장·지시 변경 때 재배정·재계획을 한다 |
| D-360 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 23. 시험·형식 검증·벤치마크 | ○ | 해석·배정 결과를 지시 시나리오 시험과 모델·프롬프트 변경 뒤 회귀시험으로 검증한다 |
| D-361 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 25. 안전·위험 관리 | ○ | 오해석이 위험한 동작으로 이어지지 않게 안전 조건을 확인 절차에 넣는다 |
| D-362 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 2. 자연어 업무 지시 챗봇 × 26. 사이버보안·접근권한·개인정보 | ○ | 채팅 사용자가 어느 로봇·구역에 어떤 작업까지 지시할 수 있는지(명령 권한)와 대화 기록 보호를 정한다 |
| D-363 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 6. 지도·공간·위치 모델 | ● | 분류 원문 10장이 건축 도면 기반 이동 지도의 중심 연구영역으로 두며, BIM·CAD에서 이동 공간을 만들고 층·목적지를 정렬하는 일 자체다 |
| D-364 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 3. 처리능력·거점·설비 계획 | ○ | 층별 지도와 공용 자원 목록이 충전기·작업대 배치와 설비 계획의 입력이 된다 |
| D-365 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 5. 로봇 능력·작업 온톨로지 | ○ | '공간 그래프로 온톨로지에 적재'한 공간·시설이 로봇 능력(계단·도어 조작·충전)과 대조된다(아이디어 1과의 연결) |
| D-366 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 8. 실시간 세계 상태·데이터 일관성 | ○ | 공간 노드(문·엘리베이터)에 붙는 현재 상태를 실시간으로 갱신하는 쪽이다 |
| D-367 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 10. 설비·건물 시스템 연동 | ○ | 인식한 문·엘리베이터가 설비·건물 시스템 연동 지점이 된다 |
| D-368 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 15. 다중 로봇 경로·교통 관리 — MAPF | ○ | 분류 원문 10장의 함께 필요한 영역(교통 관리). 공간 그래프가 경로·통로 조율의 바탕이다 |
| D-369 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 16. 공용 자원·충전·에너지 최적화 | ○ | 인식한 충전 위치·엘리베이터가 공용 자원 목록이 되어 예약·배분의 대상이 된다 |
| D-370 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 21. 온보딩·설정·현장 시운전 | ○ | 분류 원문 10장의 함께 필요한 영역(시운전). '현장 모델링 시간을 줄이는' 적용처다 |
| D-371 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 22. 시뮬레이션·예측용 디지털 트윈 | ○ | 분류 원문 10장의 함께 필요한 영역(시뮬레이션). 생성한 지도를 '시뮬레이션 초기값으로 사용'한다 |
| D-372 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 23. 시험·형식 검증·벤치마크 | ○ | 인식 결과와 생성한 지도를 시뮬레이션·실기체 주행 시험으로 검증한다 |
| D-373 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 24. 자산·소프트웨어 수명주기 관리 | ○ | 도면 개정에 따른 지도 버전 관리가 필요하다(이 영역 정의의 지도 버전) |
| D-374 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 27. AI·학습·적응과 모델 운영 | ○ | 분류 원문 8장의 교차 규칙: 도면 해석은 이 영역의 방법이 6. 지도·공간·위치 모델에 적용되는 것이다 |
| D-375 | 2026-09-25 | 트랙·아이디어: 매핑 아이디어 3. 건축 도면 자동 인식 × 28. 표준·상호운용성·다사업자 거버넌스 | ○ | 공간 그래프를 표현하는 표준(BIM·IFC, 실내 공간 표준)과 데이터 교환 형식을 본다 |
| D-376 | 2026-09-25 | 트랙·아이디어: 내비게이션 | 중점 연구 트랙 아래 트랙 3개(order 순), 그 바로 다음에 "확장 아이디어" 섹션(색인 → 아이디어 1·2·3), 그다음 주제. 4.8 순서는 그대로 | 요청. 4.8 목록 밖 섹션은 순서 사이에 끼우지 않고 트랙 옆에 둔다 |
| D-377 | 2026-09-25 | 트랙·아이디어: 내비 순서 검사 | protect_source.check_nav_structure 추가: 4.8 원문 순서 보존(부분열 순서), ideas 는 tracks 바로 다음, corrections 는 맨 뒤, 트랙은 order 순이며 첫 트랙이 맨 앞, 트랙 하위 순서(개요 → 단계 → 초안 → 산출물 → 백로그 → 로그 → 실험), 아이디어 하위 순서. 저장된 mkdocs.yml 과 build_nav() 결과 모두에 적용 | 기존 검사는 재생성 일치만 봐서 생성기 자체의 순서 회귀를 못 잡았다. 원문 순서 부분은 그대로 강제한다 |
| D-378 | 2026-09-25 | 트랙·아이디어: test_lib.py | nav 테스트 두 개를 새 구조에 맞췄다(확장 아이디어 섹션 기대, 트랙별 하위 순서를 order 순·draft_page 기준으로) | 기존 테스트가 slug 알파벳순 첫 트랙과 ontology-draft.md 고정을 전제해 새 구조에서 깨진다(담당 밖 파일이지만 run_all 통과에 필요한 최소 수정) |
| D-379 | 2026-09-25 | 트랙·아이디어: 페이지 상태 단일 원천 | auto key page-status 추가: 프런트매터에서 "> 페이지 상태 · 신뢰도 · 페이지 버전 · 마지막 갱신 · 마지막 실행" 한 줄(초안은 앞에 초안 버전, 라벨은 draft_version_label, deprecated 면 대체 페이지)을 만든다 | 요청 1-4. 손으로 쓴 줄이 프런트매터와 어긋났다(온톨로지 초안: 줄은 draft, 프런트매터는 published) |
| D-380 | 2026-09-25 | 트랙·아이디어: 상태 줄 교체 | docs 의 손으로 쓴 상태 줄 3개를 page-status 영역으로 바꿨다: 온톨로지 초안("페이지 상태:"), 7. 화물·재고·자산 식별과 추적과 주제 페이지("> 상태: draft …") | 코디네이터 추가 지시("> 상태:"로 시작하는 줄도 대상) |
| D-381 | 2026-09-25 | 트랙·아이디어: 상태 줄 검사 | render.status_line_violations(body) 를 두고 check_frontmatter 가 쓴다: page-status 영역 밖의 "페이지 상태:" 줄, "상태:"·"> 상태:"(굵게 포함)로 시작하고 값이 seed | draft verified published needs_update deprecated 인 줄을 오류로. 코드 펜스·인라인 코드는 제외. 트랙 상태·단계 상태 줄, "(상태: 열림)" 같은 질문 상태는 대상 아님 코디네이터의 스토리텔러 산출물 검사와 같은 규칙을 공유한다(import 경로 lib.render) |
| D-382 | 2026-09-25 | 트랙·아이디어: 템플릿 | area·topic·ontology-draft 템플릿의 상태 줄을 page-status 마커로 바꾸고, area 템플릿에 area-tracks 마커를 넣었다. 새 템플릿 track-draft.md·idea.md 를 만들고 README 를 갱신했다 | 템플릿이 상태 줄을 쓰라고 하면 새 검사에 걸린다 |
| D-383 | 2026-09-25 | 트랙·아이디어: 홈·소개 | 홈 7절에 트랙 3개와 확장 아이디어 연결 구조 링크, 시작하기 좋은 페이지에 색인 링크. 에이전트 소개·기여 방법에 트랙 3개와 트랙 조사 비중 설정(track_runs_per_week·track_weights)을 적었다. 읽기 가이드 자동 영역 표·아이디어 매핑 페이지 관련 페이지에 링크를 더했다 | 요청(소개 페이지와 내비게이션에 새 트랙 추가, 트랙 조사 비중은 설정값) |
